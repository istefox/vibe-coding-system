# Memory index

One line per memory. The fact lives in the linked file, not here.

- [zsh does not word-split unquoted expansions](zsh-no-word-split-for-loop.md) — `for t in $LIST` iterates once over the whole blob when LIST is space-separated and the host shell is zsh
