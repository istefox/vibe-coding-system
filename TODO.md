<!-- project-tasks: prefix=VCS lastId=81 -->
# PROJECT TASKS

Updated: 2026-09-12 · Open: 29 (P1: 0) · GitHub issues: 70

This ledger is **bilateral**: every open GitHub issue appears in the `GitHub Issues` section,
regenerated from `gh` on each run, and the sections below it hold what has no issue number yet.

That inverts the rule this header carried until 2026-08-18, which said an item with an issue number
belonged there and not here. The old rule kept the file short and made it answer half a question:
"what is open" had two answers in two places and neither was complete. Nothing is duplicated,
because the GitHub-owned fields are regenerated rather than copied, and a roadmap item carries a
pointer to its `PROJECT.md` phase rather than a transcription of it.

## GitHub Issues
- [ ] `VCS-054` -> #572 **P3** `~/.claude/skills/project-tasks/scripts/ledger-merge.sh`'s self-check rejects this ledger's own established convention: a `Backlog / To Add` line pointing to its full write-up "see Done section entry below" plus the corresponding `Done` entry both carry the same ID (observed on `VCS-038`, `VCS-049`, `VCS-050`, `VCS-051`, `VCS-052`), and the self-check's local-id-appearing-twice check treats that as a duplicate and refuses to compose a candidate (`rc=3`) — blocking every full run's write path until worked around by hand. Not a new corruption; the convention predates this observation and every one of the five flagged IDs is a legitimate pointer+closure pair, not a mistake. Needs the self-check taught to allow exactly one `Backlog`-section occurrence plus one `Done`-section occurrence per ID, rather than flagging any id appearing more than once — `~/.claude/skills/project-tasks/scripts/ledger-merge.sh`. **Correction (2026-09-07):** the target path above is stale. Per ADR-0191 (2026-09-04, this repo's own `docs/architecture/`), `project-tasks` was retired from this repo's vendored surface and its canonical source moved to `istefox/Skills` (`/Users/stefer/Developer/Skills/tasks`, symlinked at `~/.claude/skills/project-tasks`) — this ADR postdates this entry's original `opened:` date, which is why the entry still names the pre-migration path. The bug itself is presumably still real; the fix belongs in `istefox/Skills`, not here. Detailed handoff written to `~/Desktop/handoff-ledger-merge-backlog-done-dup-id.md` (2026-09-07) for a session scoped to that repo <!-- src:session opened:2026-08-30 runs:2 promote:2026-09-07 -->
- [ ] `VCS-041` -> #571 **P3** Hooking the `InstructionsLoaded` verification (ADR-0171, proposed) to the concept-to-code chain's `[FRESH SESSION]` boundary automatically — e.g. a check that blocks advancing past Gate 4 if a recent `auto-learning` correction does not show up as loaded — is out of scope for the base proposal. It touches the skill's existing gate and needs a new transition pair in the manifest state machine, not just the observability hook itself. Recorded as a possible follow-up, not started <!-- src:session opened:2026-08-27 runs:2 promote:2026-09-07 -->
- [ ] `#273` The Step 5 checkpoint has no mechanism to tell an expected red from a real one — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#293` task_num extracts digits only so a lettered task collides with its sibling — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#295` MALFORMED is dropped silently by any caller filtering BUDGET or SCOPE — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#297` the dirty-classify fence reads porcelain v1 so a renamed artifact classifies as OTHER — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#298` the collapsed snapshot commits live in the reflog only and a git gc destroys them — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#299` fence_is_abort_capable is lexical and four measured fences abort from outside it — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#301` the transcript-scan population stops at two roots — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#302` the grant-coverage guard matches a bare Bash token so an unrestricted grant in another form passes — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#303` the interpreter enumeration is a fixed list and the escapes are pinned nowhere — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#304` the reason floor is duplicated in seven derived guards and nothing stops one drifting — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#306` the deployed-only registry's reasons are true today and checked by nothing — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#307` hooks.json is outside PAIRS completeness by a deferred decision — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#308` thirteen skill-private test runners exercise the deployed copy and are dark to CI — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#309` settings.json is never synced and five guardrail hooks are wired by hand — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#315` seventeen ADRs carry the same instruction-not-an-enforcement paragraph and the class is undecided — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#335` a manifest terminal by status is exempt from invariant 4 and the state machine still lets it transition — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#336` set-branch-protection.sh unions one context while the audit derives three — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#374` No unattended run performs a review: Gate 5's autopilot default is Skip <!-- src:github state:OPEN labels: -->
- [ ] `#375` An unmarked leftover SPEC.md is mishandled in both directions: unknown routes brownfield, and the archive fence halts <!-- src:github state:OPEN labels: -->
- [ ] `#376` Step 1's slug stamp skips itself on any SPEC that mentions the marker in prose (rule 12) — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#378` usage-report.py says sub-agent token cost is not observable, reads it anyway, and mixes it into the daily total unlabelled — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#388` detect-macos.sh: a framework name in prose trips Gate 1c inside a non-UI document <!-- src:github state:OPEN labels: -->
- [ ] `#389` Five of six skippable-gate branch points have a discriminator the manifest never records <!-- src:github state:OPEN labels: -->
- [ ] `#390` The manifest's Gate 4 answer and its transition to ready_for_implementation live only in the working tree <!-- src:github state:OPEN labels: -->
- [ ] `#391` The merge-back escape check reads untracked files only, so a dispatch that ran without isolation is invisible <!-- src:github state:OPEN labels: -->
- [ ] `#398` commit Step 6b reads a dead CI watch as green: gh exits 0 when the watch dies, not only when every check passed <!-- src:github state:OPEN labels:bug -->
- [ ] `#400` autopilot-disarm clears the run scope, so a relaunch after a pause is silently unbounded <!-- src:github state:OPEN labels:chain-blocker -->
- [ ] `#402` _issue-map.tsv column order is documented in no consumer, and reading it wrong yields a plausible wrong answer — Phases <!-- src:github state:OPEN labels:documentation -->
- [ ] `#406` commit Step 5's fence cannot commit any message containing an apostrophe — Phases <!-- src:github state:OPEN labels:bug -->
- [ ] `#407` Gate 0d's git auto-detect can never take Outcome A or B — Phases <!-- src:github state:OPEN labels:bug -->
- [ ] `#413` ADR-0104 squash-merge premise is false: this repo merges with merge commits — Phases <!-- src:github state:OPEN labels:documentation -->
- [ ] `#414` Phase P step 3 and project-conductor use different predicates for SPEC coverage — Phases <!-- src:github state:OPEN labels:bug -->
- [ ] `#415` Phase 0 check 9 file-wide --only matcher aborts on issues whose number appears in a PROJECT.md heading <!-- src:github state:OPEN labels:chain-blocker -->
- [ ] `#417` The tester brief specifies what to assert and never what a good assertion is — Phases <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#418` Context is instrumented and is never a decision: the chain names a phase boundary exactly once — Phases <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#419` Budget: measures diff lines, and the axis that actually ends runs is context — Phases <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#420` The chain has no wayfinding phase: nothing gets an idea from loose to formulable — Phases <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#421` No entry point chooses between concept-to-code, autopilot and wayfinding — Phases <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#422` Express E4 and Hybrid H5 never commit the chain manifest at all — Phases <!-- src:github state:OPEN labels:bug -->
- [ ] `#426` settings.json drift runs in BOTH directions: the deployed and staged copies each register a `Stop` hook the other lacks, and `mark-dirty.sh` keeps producing markers for a consumer that was never wired — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#427` Three items left open by #404: the .git worktree gate, an unasserted STOP_GATE_TEST_TIMEOUT=0, and an exclusion list that now covers tracked content — Phases <!-- src:github state:OPEN labels:prep -->
- [ ] `#428` run-hook-tests.sh asserts over the retired backup-before-deploy.sh, and no gate executes the file so the red has gone unobserved <!-- src:github state:OPEN labels:prep -->
- [ ] `#432` permission-mode-state.sh returns UNOBSERVABLE after any /clear and blames the build, so autopilot Phase M refuses a correctly configured machine <!-- src:github state:OPEN labels:chain-blocker -->
- [ ] `#436` The overwrite invariant lost its only backstop: 2.1.228 lets newer models Write over an unread file, and protect-files.sh is a secrets path denylist, not a diff gate — Phases <!-- src:github state:OPEN labels:bug -->
- [ ] `#437` Hand-rolled deployment is the common cause behind #426, #309 and VCS-022: staging/plugin is already a plugin, and 2.1.224's archive source with SHA-256 pinning would make staged and deployed one artifact — Phases <!-- src:github state:OPEN labels:enhancement,prep -->
- [ ] `#439` The chain discards the conversation and never runs the product, which is why /goal lands closer: a single-session lane with executable acceptance — Phases <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#440` PROJECT.md is silent about 44% of open work: the roadmap grows by audit, so 27 open issues have no phase and nothing runs the list backwards — Phases <!-- src:github state:OPEN labels:documentation -->
- [ ] `#445` F4 accepts a marker mention as proof a fence was executed, and cannot tell an extractor from a grep <!-- src:github state:OPEN labels: -->
- [ ] `#454` spec_topic_match has three values and the code has two: an unverifiable SPEC is treated as owned — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#457` An in-flight manifest turns CI red between Gate 4.0 and Step 7, and no ADR names the window — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#458` A red harness loop skips the union steps, so the registry's coverage check has never run in CI <!-- src:github state:OPEN labels: -->
- [ ] `#459` The chain-history event log records shell tokens as manifest states, in 6 of 61 records — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#460` Step 7.0b enrols a SPEC in the scope-baseline corpus and nothing bumps the baseline — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#461` The autopilot routing pre-flight tests for a SPEC.md file, and gate0-detect.sh can disown the file it finds — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#462` autopilot check 9 normalises every PROJECT.md line, so a table row collides with a 40-character feature slug — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#463` No autopilot pre-flight check asserts the per-feature SPECs ADR-0022 requires actually exist — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#464` gate0-detect.sh detects ADRs by one naming convention, so an adr-tools repository reports adr=no — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#465` Two divergent derivations of the same feature slug, and the branch, manifest and publish names depend on which wins — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#466` detect-macos.sh strips tables and code spans, so a macOS SPEC that names its platform only there reports NOT_MACOS — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#469` Nothing gitignores .claude/autopilot-state/, so a run's transient state lands in the PR it opens — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#475` assigned-secret: the prose false positive is a keyword followed by an absolute path, and it occurs 0 times in 423 files — Phases <!-- src:github state:OPEN labels:bug -->
- [ ] `#480` The conductor's slug and the SPEC's stamped slug are derived from different inputs, and disagreement is read as a disowned SPEC — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#481` agent-metrics.sh's test_count_delta counts test files, not tests, so it reports 0 for every change to an existing harness <!-- src:github state:OPEN labels: -->
- [ ] `#482` spec-coverage.sh's rc=2 and diff-budget-check.sh's CLEAN both make a wrong CWD indistinguishable from a passed gate <!-- src:github state:OPEN labels: -->
- [ ] `#483` weakening-scan's zero-assertion-test fires on every pytest.raises test: the assertion-token list has no raises idiom — Phases <!-- src:github state:OPEN labels: -->
- [ ] `#484` No launcher for the arming ritual: five preconditions checked by hand, each able to fail the run late — Phases <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#485` ADR-0154 §D7's residual is not narrowable by filtering: ignoring fixture-borne ids costs 0 and removes 0 <!-- src:github state:OPEN labels: -->
- [ ] `#558` Step 5 memory shard curation never produced `.claude/agent-memory/coder/MEMORY.md` <!-- src:github state:OPEN labels: -->
## Open Issues

- [ ] `VCS-027` **P2** POINTER ONLY, deliberately not a copy: the `brief-to-app` build lane is issue **#439** and its three waves are `PROJECT.md` Phase 13. Recorded here against this ledger's own rule — which says an item with an issue number leaves the file — because the operator asked for a pointer and the alternative was duplicating the issue body, which is the second source of truth ADR-0024 forbids. Nothing but the two references belongs on this line; read #439 for the problem and the plan, Phase 13 for the wave order. Delete this line when Phase 13's last wave closes <!-- src:session opened:2026-08-15 runs:6 -->
- [ ] `VCS-031` **P2** `ledger-merge.sh`'s roadmap phase pointer is uniformly WRONG on this repo, not absent — it reads a level-two heading pattern for the heading above a row, but `PROJECT.md`'s 24 phase headings are `### Phase N — …` and its only level-two headings are `Overview`, two `Where this stands` blocks and `Phases`. Measured 2026-08-19 by replaying the awk: **203 of 203 rows resolve to the literal string `Phases`**, the container, and 0 to a phase number, so every GitHub Issues line would gain `— Phases`, which is the section they are all in and therefore says nothing while looking like information. Rule 18's shape: the scan is satisfied by a population larger than the one it meant. Worked around by omitting `--roadmap`, which degrades with a stated notice as the contract promises. The fix is one pattern (`/^#{2,3} /` or `/^### Phase /`), but it needs a denominator guard too (rule 7): a pointer map that resolves 0 rows to a phase must report that, not emit a label. **Update 2026-09-11:** the vendored-twin path below is stale — per ADR-0191, `project-tasks` was retired from this repo's vendored surface entirely (same precedent as `VCS-054`); the only surviving copy is `~/.claude/skills/project-tasks/scripts/ledger-merge.sh` line 135, symlinked from `istefox/Skills` (`/Users/stefer/Developer/Skills/tasks`), outside this session's scope — the fix belongs in that repo, not here — `~/.claude/skills/project-tasks/scripts/ledger-merge.sh` line 135, and its vendored twin `staging/plugin/skills/project-tasks/scripts/ledger-merge.sh` <!-- src:session opened:2026-08-19 runs:7 -->
- [ ] `VCS-062` **P3** ADR-0192's deployed `coder.md` did not fully hold its own Hard rule 2 on the very first attempt of a live smoke test: the coder's first tool call was a `Write` to an ABSOLUTE path into the shared checkout (no `pwd` run first), correctly blocked by the worktree guard, then self-corrected to a relative-path write with a `PATTERN:` header on both subsequent real writes. Observed 2026-09-05 by reading the subagent transcript directly (`agent-a9675ea0896573704.jsonl`), not the coder's self-report. This is exactly the "instruction, not enforcement" gap ADR-0192 §Consequences names and pre-registers a re-measurement for — one data point, not yet a pattern; re-check against the 30-dispatch criterion the ADR already sets <!-- src:session opened:2026-09-05 runs:2 -->
- [ ] `VCS-064` **P3** Stefano flagged that `codex-reviewer.sh`'s review dispatches were burning too much Codex quota — the script never overrides model or reasoning effort, so every call (RTF Step 1/advisor/Step 4, both Step 5 checkpoint sites) inherited his global `~/.codex/config.toml` interactive default, `gpt-5.6-sol` at `model_reasoning_effort = "xhigh"`, the top-cost combination on that config. Checked live against `~/.codex/models_cache.json` (fetched 2026-09-05T17:28:41Z) rather than assumed from training data (rule 13): confirmed `gpt-5.6-terra` ("Balanced agentic coding model for everyday work") exists in the current 5.6 family alongside `sol` and the cheaper `luna`. Pinned in `codex-reviewer.sh`'s `codex exec` call: `-m gpt-5.6-terra -c model_reasoning_effort=medium`, scoped to this one invocation only — Stefano's global config and interactive Codex sessions are untouched. Re-verified `codex-reviewer-schema.test.sh` green (3/3) after the edit; no other test asserts the exact invocation line. Stefano then asked for a live dry-run measuring actual subscription consumption. Read live from `~/.codex/sessions/<Y>/<M>/<D>/rollout-*.jsonl` `token_count` events (no `codex usage` command exists): one `--mode review` call over a real ~230-line uncommitted diff consumed 45k-90k tokens per attempt while `rate_limits.primary.used_percent` stayed at 0.0 on the Pro plan — negligible. The dry run also surfaced and confirmed a genuine wrapper defect: `codex exec`'s stderr echoes the full prompt it was sent, and the reviewed diff (which included `codex-reviewer.sh`'s own rate-limit-vocabulary grep line) contained the literal words the wrapper's own rate/quota check greps for — so a completed, paid-for review was discarded and reported as `DID-NOT-RUN`. Root-caused by re-running with stderr preserved instead of deleted. Fixed by reordering the checks: a non-empty `$RAW_OUT` (real output) now outranks the stderr grep, which only runs as a fallback diagnosis when there is no output at all. Re-verified: the same dry run now returns `EXIT:0` with a real review (3 MAJOR + 1 MINOR findings, used to find the two VCS-063 corrections above); `codex-reviewer-schema.test.sh` still 3/3. **Not yet done:** a planted regression test for the check-ordering fix itself (currently verified live but not pinned by an assertion) <!-- src:session opened:2026-09-05 runs:2 -->

**Fix roadmap (2026-09-05, sequencing for VCS-060/061/062):**
1. ~~`VCS-061` first, no dependency on the others~~ — **done**, see Done section (2026-09-05).
2. ~~`VCS-060` next, independent of `VCS-061`~~ — **done**, see Done section (2026-09-11): the needle was never stale, PC2 fired clean on a live re-run, no code change needed.
3. Project Map drift correction — now unblocked (`VCS-061` fixed, `scan.sh` runs clean from this worktree). Run `scan.sh --root` and diff its `MAP` records against `## Project Map` below; apply only the lines that actually changed. Not yet done — still needs a dedicated pass, not a hand-edit of the counts.
4. `VCS-062` has no fix step — defer to a re-measurement after 30+ real coder dispatches, per ADR-0192's own pre-registered success criterion (§Consequences).

- [ ] `VCS-079` **P2** Emdash sandbox injects a git credential-helper for `github.com` via `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_n`/`GIT_CONFIG_VALUE_n` pointing at an unreachable local proxy (curl rc=7), breaking `git push` with a misleading "Device not configured" error even though `gh auth git-credential` is correctly configured in `~/.gitconfig`. Worked around by unsetting those vars for the push call; an issue draft for Emdash's own repo is ready but not yet submitted (`~/Desktop/emdash-git-push-credential-helper-issue.txt`, `~/Desktop/emdash-git-push-actual-vs-expected.txt`) <!-- src:session opened:2026-09-11 kind:fix runs:1 -->

- [ ] `VCS-004` **P3** the autopilot run report is gitignored, so a run's spend and per-feature metrics survive only on the machine that produced them — `.claude/autopilot-report.json` (renamed from `nightly-report.json` by ADR-0127; a `file-missing` STALE record under the old name is that rename, not a closure) — now also carried as PROJECT.md Phase 11 Wave 5, where Wave 1's budget recalibration depends on it <!-- src:session opened:2026-08-04 runs:6 -->
- [ ] `VCS-025` **P3** the #312 plan states `staging/plugin/scripts/tests/` is PAIRS-covered "except for four named legacy files"; measured 2026-08-14 there are **six** entries under `plugin/scripts/tests/` in `sync-to-claude.sh` (`phase1.test.sh`, `prep.test.sh`, `hook-verify-workflow.test.sh`, `db-backup-guardrail.sh`, `pre-flight-pattern-enforce.sh`, `run-hook-tests.sh`). The conclusion the plan drew is unaffected — a new `.tsv` there still needs no entry, confirmed by a green `pairs-completeness.test.sh` — so the plan is NOT corrected in place (rule 14, it is a historical record). Recorded here because the correction otherwise lives only in `.claude/step5-report.json` and `.claude/autopilot-report.json`, both gitignored per `VCS-004`, and the "four" figure will be copied forward by the next plan that quotes it <!-- src:session opened:2026-08-14 runs:6 -->
- [ ] `VCS-026` **P3** verifying a post-collapse worktree branch by patch identity gives a confident WRONG answer, and it is the answer that blocks cleanup. Measured 2026-08-14 on the 17 `worktree-agent-*` branches left by the #312 run: `git cherry main <branch>` reported **17 of 17 with 100% unique patches** and no tip was an ancestor of `main`, because Step 7's snapshot collapse (ADR-0104) rewrites the commits and so changes every patch-id. The blob-level check inverted it — of ~660 files per branch, **0 to 2** blobs were absent from the 4921 reachable from `main`, and every one was an intermediate draft of six paths whose final version is in `main`. Both `git branch --merged` and `git cherry` answer "is this commit in main", which after a collapse is not the question; the question is "is this content in main". Use `git rev-list --objects main` versus `git ls-tree -r <branch>`. Recorded because the wrong tool is the one a person reaches for first, and it argues for cleanup being skipped indefinitely — related to issue #298. **Update 2026-08-19: the cleanup happened, and this entry's own prescription was not followed.** All 18 `worktree-agent-*` branches plus the four merged feature branches were deleted, local and remote, after `gh` confirmed every feature branch's PR was MERGED. But the branch-level decision used `git cherry` (which reported 1 to 7 unique patch-ids on 10 of 18, exactly the collapse artefact this entry names) plus a heuristic on commit subjects — every unique commit carried a mechanical `chore(step5): snapshot` subject and none carried human work. The blob check this entry prescribes was run only AFTERWARDS, against the recorded shas: **11 blobs absent from `main` across 5 paths** (`spec-coverage.test.sh` x5, `PROJECT.md` x3, `stop-gate-path-predicate.test.sh`, `batch-boundary-precedence.test.sh`, `ADR-0155`), and all 5 paths are present in `main` in their final version, so the 11 are intermediate drafts and nothing was lost. The prediction held; the method did not get reached for at the moment it mattered. What is still missing is anything that surfaces this entry at deletion time — the shas are in the session scratchpad only <!-- src:session opened:2026-08-14 runs:6 -->
- [ ] `VCS-030` **P3** `.claude/test-timeout` is 600s against a suite measured at **415s over 85 harnesses** on an unloaded machine — 1.45x, thinner than the 2x this file has carried before (159s -> 300s). Set deliberately by the operator on 2026-08-19 over the convention's ~830s, to cap a hung suite at 10 minutes rather than 14. The risk is stated rather than discovered: 415s was measured with nothing else running, and concurrent agent builds (the contention ADR-0159 addressed) grow it. The value has already fallen below its own suite once, by growth and not by error — ADR-0156 flagged it at 79 harnesses when there were 85. If a timeout reappears, re-measure before raising; ADR-0137's `TMO_MAX` caps any value at 900 <!-- src:session opened:2026-08-19 runs:6 -->
- [ ] `VCS-032` **P3** The autocompact threshold TRACKS `autoCompactWindow`, which was asserted twice before it was measured. Session of 2026-08-21, `compactMetadata.preTokens` on every `compact_boundary` record: under a 300k window, 18 compactions between 07:00:19 and 08:14:22 UTC — the first at 140,753, then a flat **83,898-89,988** seventeen times in 74 minutes, which Claude Code itself reported as thrashing. The window was raised to 600k at 08:22:20 UTC; the single compaction since, at 08:54:02, reads **179,409**. Doubling the window moved the trigger 2.11x, so the key governs and the earlier "it does not" reading was wrong. What is NOT established is the function: 179,409/600k and ~85,000/300k are both ~29%, but the first compaction of the day (140,753 against 300k, 47%) does not fit, so the ratio is an observation over one sample per regime and must not be stated as a mechanism — ADR-0162 §D4 says the same thing at the enforcement point. The method error is the entry: the value was changed twice on a reading the data did not support, and the readout cost one command. The remaining lever is the ~50k fixed preamble re-created after each compaction (`cache_creation_input_tokens` 48,456-54,945 on the first turn after, 546-2,008 after that), of which the ADR index in `CLAUDE.md` was ~5.9k per turn — moved out to `docs/chain-decision-index.md` on 2026-08-21 by ADR-0163, taking `CLAUDE.md` from 35,715 to 12,368 bytes, while ADR-0162 stopped the `SessionStart` hook re-injecting the project context at every compaction. Neither is yet measured against a fresh session's first-turn `cache_creation_input_tokens`, which is the only number that closes this entry. Method note for the next reader: transcript timestamps are UTC and `stat` is local, and mixing them cost a wrong conclusion about when the setting was written <!-- src:session opened:2026-08-21 runs:5 -->
- [ ] `VCS-080` **P3** `~/.claude/skills/project-tasks/scripts/scan.sh`'s file-missing STALE heuristic false-positives on non-path code spans (e.g. `compactMetadata.preTokens`, `experimental.cacheTtl`) and on files that moved but kept the same basename, 15 of this run's 16 STALE candidates were false positives, only `VCS-029`'s stale `.github/workflows/ci.yml` reference was real. Fix belongs in `istefox/Skills` per `VCS-054`'s precedent, not here <!-- src:session opened:2026-09-11 kind:fix runs:1 -->

## In Progress

- [ ] `VCS-076` **P2** Codex CLI backend for the `coder` subagent (Stefano's explicit request, `docs/manifests/2026-09-07-codex-coder-backend-choice.manifest.yml`, standard chain). SPEC.md, `ADR-0196-codex-coder-choice.md` and `docs/superpowers/plans/2026-09-07-codex-coder-backend-choice.md` complete and approved (Gates 1-4). Unlike ADR-0194's tester, the coder gate exposes a live Codex model choice (Astra `gpt-6-astra` / Sol `gpt-5.6-sol`, both effort low-ultra) and never skips the ask (no `*_asked` field — a fresh choice at every Step 5 entry, resumed included, per Stefano's explicit requirement). Five parity gaps documented, not silently dropped: four Claude `PreToolUse` hooks (`pre-flight-pattern-enforce.sh`, `write-scope-enforce.sh`, `test-write-scope.sh`, `coder-memory-scope.sh`) cannot fire on a `codex exec` subprocess — mitigated by `-s workspace-write` sandbox + a post-hoc 5-class blocklist scope check (new exit 4), not restored; no per-call PATTERN classifier (replaced by a post-hoc `pattern_classification` field); `LSP` structurally absent from Codex (native Claude Code capability, not an MCP server); `eslint` MCP source unlocated on the verifying machine; `context7` MCP pre-registered in Codex but unauthenticated. Remaining: Step 5 implementation (`codex-coder.sh`, IF/ELSE at both `step5-implementation.md` dispatch sites, manifest fields `use_codex_coder`/`step5_codex_coder_model`/`step5_codex_coder_effort` + Invariants 28/29, `codex-coder-dispatch-gate.test.sh`) — deferred to a fresh session per the Standard chain's own session boundary (Gate 4). <!-- src:session opened:2026-09-07 kind:roadmap -->
- [ ] `VCS-077` **P3** `codex-common.sh` extraction: the Codex availability cascade (`codex doctor --json` check, rate-limit vocabulary regex, VCS-064's check-ordering fix) now exists independently in three scripts (`codex-reviewer.sh`, `codex-tester.sh`, and — once VCS-076 ships — `codex-coder.sh`). ADR-0194 §R7 deferred extraction at two copies; ADR-0196 §R9 flagged that the deferral argument is now weaker at three. Flagged to Stefano at ADR-0196's Gate 2, who chose to defer rather than fold it into that ADR's scope. Needs its own small ADR before extraction (rule 6: a shared source that breaks disables three consumers at once). <!-- src:session opened:2026-09-07 kind:roadmap -->

## Backlog / To Add

- [ ] `VCS-073` **P2** ADR-0194 §D5: `plant-shard` is ~57 of every ~66 billed CI minutes (86% of a PR's cost), running all 674 declared plants on every full-tier run regardless of what changed — a one-line docs edit costs ~70 billed minutes. Design a selective-plant scope: run only the plants whose target file (or declaring harness) is in the changed set, plus their shared-helper closure, estimated (not measured) to bring a typical run from ~66 min to ~10-15 min. Must resolve two things ADR-0151 §D10 already flagged as hard for a narrower version of the same idea: a plant that was not selected must report a state distinct from a clean result (rule 4), and the selection's own denominator must be guarded (rule 7) plus a periodic full sweep (`schedule:`/`workflow_dispatch`) to catch couplings the closure misses. Needs its own ADR before design; the ~10-15 min estimate is derived from plant counts and per-plant timing, not a built prototype, and must be re-derived first (rule 13) <!-- src:session opened:2026-09-06 runs:1 -->
- [ ] `VCS-057` **P2** Stefano asked how to structurally fix `coder`'s context rot after VCS-056/ADR-0183 excluded it from native memory on a measured-but-incomplete basis (bare dispatch, never exercised Step 5's own merge-back). Two-phase plan approved via plan mode (`/Users/stefer/.claude/plans/zippy-whistling-crescent.md`). **Phase 1 (this entry, implementation in progress):** re-measured live in a throwaway scratch repo (`~/Developer/_scratch/vcs057-coder-mergeback/`, six runs M1-M6) — confirmed a coder's `memory: project` write DOES persist onto the feature branch via Step 5's merge-back and IS re-injected on the next dispatch, but two parallel coder dispatches sharing one `MEMORY.md` produce a real merge conflict (measured, not assumed). Implemented: new guard `coder-memory-scope.sh` (CM1-CM8, all plants verified to flip RED, including a genuine symlink-escape defect the guard's own test-writing process caught before shipping — gate on the RAW file_path for scope, resolved path for shard membership); `agent-memory-contract.test.sh` rewritten AM2→AM2′ (allowlist of one, `coder`, per rule 19 — old absolute-ban comment kept, not deleted) plus new AM6 (producer/consumer meet, rule 17: `coder.md`'s memory: field, the guard, and its `sync-to-claude.sh` wiring must all exist together — plant verified after tightening the check from a loose substring match to the exact PAIRS-line match, since the loose form was a genuine NOFIRE); wired into `sync-to-claude.sh` (PAIRS + MANUAL STEP + settings block), `sync-manual-steps.test.sh`'s `WIRED_HOOKS`, and `docs-ci.yml`'s test list (48/48 and 7/7 passing respectively). `coder.md` now carries `memory: project` plus a "Memory write scope" section instructing the per-dispatch shard discipline; both Step 5 coder prompt sites (Workflow Stage 2, Agent-tool Single batch dispatch template) updated to match, plus a new "Memory shard curation" sub-step (orchestrator-only, once per Step 5 run, after the last merge-back) and a note on the ESCAPE CHECK explaining why a coder's shard write is invisible to it by design. Recorded as **ADR-0184** (per-feature-branch memory scope by construction via `baseRef: "head"`, no shard pruning until the branch merges to `main`, shards stay tracked or the merge-back mechanism itself breaks), with a dated `## Correction` appended to ADR-0183 (not edited in place, rule 14) and a forward-pointing note in `docs/vibe-coding-system.md`. **Phase 2 (preamble reduction, not started):** re-derive the real `Budget:` declaration coverage with `plan-task-predicate.awk` (two independent rough counts so far disagree, 109 vs 157, and a first attempt with the real predicate returned 0 — must be resolved before deciding whether the L2 budget-driven batch-sizing leg proceeds at all), then `step5-brief.sh` (L1: byte-exact plan-slice briefs with backwards coverage verification) and, if the budget count supports it, budget-driven batch sizing (L2). **Phase 1.A verification (2026-08-31):** full co-resident guard regression green (`coder-memory-scope` 8/8, `agent-memory-contract` 7/7, `reviewer-write-scope` 6/6, `test-write-scope` 55/55, `memory-store-guard` 4/4, `write-scope-enforce` 18/18, `scope-guards` 30/30, `sync-manual-steps` 48/48, `pairs-completeness` 337/337, `cross-reference-form` 38/38, `transition-producer` 19/19, `ci-tier`/`ci-tier-workflow-decide` 28/28 + 11/11), `markdownlint-cli2` clean, full `plant-check.sh` sweep green (662/662, all 654 plants fired) after fixing three real defects the sweep itself caught (rule 2): AM2′'s plant needle used a literal `\n` — only the REPLACEMENT field interprets that escape, not the needle, which is matched word-by-word; AM6's declared plant path was `staging/sync-to-claude.sh` instead of the staging-relative `sync-to-claude.sh`, resolving to a nonexistent double-nested path and caught independently by `ci-tier.test.sh`'s SKF1b/SKF2 via the Stop-gate hook; and my own edit to the Agent-tool fallback's transition line accidentally joined "transition to" and `` `step_6_review`. `` onto one physical line, creating an unintended second producer that made `transition-producer.test.sh`'s TP1 plant a NOFIRE — fixed by restoring the original two-line wrap the file's own design comment explains is load-bearing for exactly-one-producer. Deployed (`sync-to-claude.sh --apply`) and hook wired into `~/.claude/settings.json` (backed up first, `settings.json.bak-20260831-155946`). **Explicitly skipped, by Stefano's decision:** the live two-coder Step 5 fan-out re-verification in the scratch project — I cannot dispatch agents into a different project from this session (agent registry / worktree context is bound to this session's cwd), and Stefano chose "commit now" over running it himself. The automated coverage (CM1-CM8, AM0-AM6, the live M1-M6 merge-back measurement already run earlier, and the full plant-check sweep) is the evidence base for this decision; the live fan-out was additional confidence, not the only evidence, but was never actually exercised end-to-end post-deployment. **Phase 2 L1 (2026-08-31 → 2026-09-01):** `step5-brief.sh` (write/`--verify`/`--digest`) shipped and merged (PR #543, #544; ADR-0185). **Phase 2's blocking `Budget:` coverage question is now resolved (2026-09-01):** re-derived with the real predicate (`plan-task-predicate.awk` as task-block boundary, `plan-budget-parse.awk` as parser) over the 79 plans in `docs/superpowers/plans/` — 672 task blocks total, **159 with a parseable `Budget:` (23.7%)**, 0 `MALFORMED`, median declared ceiling 70 lines, 7 fully-budgeted / **16 mixed** / 54 unbudgeted plans. Both earlier rough counts (109, 157) were wrong for the same reason: they counted ceilings per FILE instead of summing them per TASK, which is what `parse_budget()` actually does. **L2 (budget-driven batch sizing) proceeded** on this measurement — implemented as `step5-brief.sh --suggest-batches` (ADR-0186), Agent-tool dispatch path only (scope decision: ~94% of runs per `dispatch-state.sh`). Corpus-verified invariant: 0 cases of a batch bigger than today's fixed `ceil(openers/3)` rule, across all 79 plans. **Post-commit fix (2026-09-01, PR #545 still open):** a `codex review --commit` probe (an evaluation, not an adopted mechanism) found `--suggest-batches` could print a letter-suffixed task ("Task 1b") as a range endpoint ("1-1b"), which write mode's numeric-only `--tasks` parser rejects, making the batch undispatchable — reproduced by hand before acting on it. First fix attempt (print only the integer prefix) was verified by hand and found unsound: it silently dropped the lettered task's content from the dispatched slice. Shipped fix instead isolates a lettered task as its own standalone singleton (never opens/extends a range) and widens write mode's own singleton case to accept one letter-suffixed designator, which is the "request it as a separate brief" remedy write mode's own range-rejection message already promised. Two latent bash-arithmetic bugs in write mode, exposed only once letter-suffixed singletons became acceptable, fixed alongside it. Verified end to end on the real corpus plan: every printed range round-trips through write mode, union of dispatched task headings matches the plan's real task set exactly, no gap, no duplication. New tests `step5-brief.test.sh` SB29-SB32 (corpus regression, synthetic budget-boundary fixture, full round-trip, write-mode singleton acceptance/rejection), `Z1` bumped 34→38, ADR-0186 amended in-flight (PR unmerged, rule 14) with a new decision paragraph. <!-- src:session opened:2026-08-31 runs:2 -->

**Claude Code CLI changelog roadmap (2026-09-06, range 2.1.246–2.1.263 vs. the prior analysis pinned at 2.1.245):** eight candidates for review against the blueprint, none yet confirmed as defects in this repo's code — each needs its own look before becoming a fix entry or an ADR follow-up.
- [ ] `VCS-065` **P3** Re-check the blueprint's path-scoped rules (`.claude/rules/*.md` with `paths:` frontmatter) and Edit/Write/Read permission patterns against two now-fixed CLI bugs: 2.1.260 dropped/ignored `Edit`/`Write`/`Read` rules whose path contains parentheses, silently leaving cartelle previste come read-only scrivibili; 2.1.259 had `Read()` deny rules not covering `--ignore-revs-file=.env`-style option values, `git diff`/`git grep` file operands, or `cd DIR && cat FILE` compounds. Both are fixed as of 2.1.263 — the task is verifying whether any existing rule in this repo was silently exposed by either gap before the fix landed <!-- src:session opened:2026-09-06 runs:1 -->
- [ ] `VCS-066` **P3** Evaluate adopting the new `PreModelSwitch`/`PostModelSwitch` hook events (added 2.1.251) in sec. 7 (hooks) of `docs/vibe-coding-system.md` — could give the blueprint a governance point for model changes mid-sub-agent-dispatch that does not exist today <!-- src:session opened:2026-09-06 runs:1 -->
- [ ] `VCS-067` **P3** Run `/skill-doctor` (added 2.1.261) against this repo's deployed skill set to find unused skills and their context cost — possible input for a future `claude-md-slim`-style pass or a skill-pruning decision <!-- src:session opened:2026-09-06 runs:1 -->
- [ ] `VCS-068` **P3** Review sec. 2 (agent-teams) and sec. 3 (sub-agents) against three SendMessage/agent-team fixes: 2.1.251 fixed a teammate's final answer not reaching the team lead, and background sub-agents unable to reply to a message from an unnamed sibling/parent (`from` carried the agent type, not a valid address); 2.1.260 fixed a sub-agent resumed via `SendMessage` never being woken by the resumed agent's completion, and a backgrounded session appearing twice in `ListAgents`. Check whether the blueprint documents any inter-agent messaging pattern that assumed the old (broken) behavior <!-- src:session opened:2026-09-06 runs:1 -->
- [ ] `VCS-069` **P3** Review `coder`'s `isolation: worktree` guidance against repeated worktree-isolation false-refusal fixes: 2.1.257 and 2.1.259 both fixed worktree-isolated sessions refusing legitimate Bash loops, xargs pipelines, launcher-wrapped commands, `$VAR` reads, `"$(…)"`, and heredocs as "too complex to verify"; 2.1.257 also fixed sandboxed git commands in a linked worktree losing write access to the repo's common `.git` after a `cd` into a subdirectory. Check whether any workaround for these now-fixed limitations is still documented in `coder.md` or ADR-0068 and can be retired <!-- src:session opened:2026-09-06 runs:1 -->
- [ ] `VCS-070` **P3** Evaluate `--permission-prompts none` (added 2.1.259, for unattended headless hosts — anything that would prompt is denied automatically while the active permission mode still decides) as a hardening option for autopilot/headless runs (sec. autopilot, ADR-0022) <!-- src:session opened:2026-09-06 runs:1 -->
- [ ] `VCS-071` **P3** Evaluate `experimental.cacheTtl` (`"5m"`/`"1h"`, added 2.1.248) in sub-agent frontmatter as a cost lever for sec. 3.9's model-choice cost/capability balance, per-agent instead of only at the subagent-model-setting level <!-- src:session opened:2026-09-06 runs:1 -->
- [ ] `VCS-072` **P3** Check whether `docs/vibe-coding-system.md` or any skill still describes the pre-2.1.246/2.1.248 behavior of `/loop`/`/goal`: 2.1.248 made `/loop`'s self-paced dynamic mode and no-prompt autonomous default always available (including on Bedrock/Vertex/Foundry); 2.1.246 capped `/goal` idle sessions at three check-ins on long-running background work per goal (three more per new user message). Relevant to the autopilot section's description of these commands <!-- src:session opened:2026-09-06 runs:1 -->

## Initiatives (multi-ADR)

- [ ] `VCS-074` **P3** Mapped feasibility of delegating Codex to agents beyond `reviewer` (already substituted per ADR-0187/ADR-0193): `debugger`, `tester`, `refactorer`, `researcher`, `coder` — no decision taken yet, needs an ADR before implementation. **Update 2026-09-07:** `tester` decided and implemented (ADR-0194, `codex-tester.sh`); `coder` decided (ADR-0196, `VCS-076`) — ADR + plan complete and approved, Step 5 implementation (`codex-coder.sh`, dispatch-site wiring, manifest fields, test harness) deferred to a fresh session per the chain's own session boundary. `debugger`/`refactorer`/`researcher` remain undecided. <!-- src:session opened:2026-09-07 kind:roadmap -->
  - Read-only pattern (`-s read-only`, same shape as `codex-reviewer.sh`) vs write-capable pattern (`-s workspace-write`, verified live on installed codex-cli 0.153.4) determine which agents are safe to delegate without new guardrails.
  - | Agent | Pattern | Current Claude cost | Token savings | Cross-model verification value | Risk | Priority |
    |---|---|---|---|---|---|---|
    | architect | A (or side-by-side) | opus + effort xhigh, priciest single dispatch | high per dispatch, low frequency | high but delicate — mistakes propagate downstream | low (never writes code) | scope unclear: substitution vs second opinion are different goals |
    | debugger (diagnosis phase) | A | sonnet + effort high, variable | good | high and direct — independent root-cause hypotheses | low | readiest to implement, reuses `codex-reviewer.sh` shape |
    | tester | B | sonnet + effort xhigh (now high, ADR-0194) | high | medium-low but real | medium (mitigated by pass/fail oracle) | **done — ADR-0194, `codex-tester.sh`** |
    | refactorer | B | sonnet + effort medium | low | near zero (snapshot harness already verifies) | high on production code | low |
    | researcher | A (already read-only) | haiku + effort low, already cheapest | minimal | low | low | low |
    | coder | B | sonnet + effort xhigh | high (Astra/Sol, medium-ultra) | high — Stefano cares particularly about coder-generated code quality | highest of the five; sandbox + post-hoc blocklist mitigates, does not restore preventive parity | **decided — ADR-0196 (`VCS-076`), Step 5 impl pending** |
  - ADRs: ADR-0194 (`tester`, done), ADR-0196 (`coder`, ADR+plan done, implementation pending — `VCS-076`)
  - Phase: in-progress (2 of 5 agents decided; `debugger`/`refactorer`/`researcher` remain)

## Blocked / Decisions Needed

_none_

## Steps — project-tasks-vendored-bilateral-ledger (derived from 2026-08-17-project-tasks-vendored-bilateral-ledger.manifest.yml)

<!-- steps: no non-terminal manifest under docs/manifests — section omitted -->
## Project Map

- **Entry point**: not a code project. `docs/vibe-coding-system.md` is the blueprint; `staging/` is the deployable surface synced into `~/.claude` by `staging/sync-to-claude.sh`
- **Modules**: `staging/plugin/skills/` (32 skills) · `staging/plugin/scripts/` (hooks) · `staging/plugin/agents/` · `staging/plugin/scripts/tests/` (90 harnesses + the plant registry) · `docs/architecture/` (172 ADRs) · `docs/specs/` · `docs/superpowers/plans/` · `docs/manifests/`
- **Build & test**: no build, no lint, no package manager. test-cmd: `for t in staging/plugin/scripts/tests/*.test.sh; do bash "$t" || exit 1; done`. Mutation testing: `bash staging/plugin/scripts/tests/plant-check.sh`
- **Key ADRs**: ADR-0022 nightly-autopilot · ADR-0068 worktree isolation contract · ADR-0086 when a derived guard is extracted · ADR-0108 the plant registry · ADR-0114 required-checks audit
- **Invariants**: merge stays human, always · a check that did not run must be distinguishable from a check that found nothing (exit 3) · a checker branches on its exit code, a reporter always exits 0 and prints `CLEAN` · an assertion nobody planted pins nothing · a needle must belong to the mechanism, not to the prose describing it · measure an issue's own claim before designing

## Done

- [x] retire `usage-daily-hint` hook and its `context-occupancy.sh` dependency (Stefano's explicit instruction) <!-- src:session kind:fix --> Deleted `staging/plugin/scripts/{usage-daily-hint.sh,context-occupancy.sh}` (context-occupancy.sh had exactly one runtime caller, verified) and the already-deployed `~/.claude/hooks/{usage-daily-hint.sh,context-occupancy.sh}` copies by hand. Removed both `sync-to-claude.sh` PAIRS entries and the now-obsolete "hook wiring" MANUAL STEP block; added a "retired context-occupancy hook cleanup" MANUAL STEP block (modeled on the existing agent-notes-harvest.sh precedent) for any other checkout still carrying the deployed files. Trimmed `precompact-occupancy.test.sh` to cover only the PreCompact guard (§D1-D3, unaffected and still active/wired) — deleted §D4/D5 assertions explicitly rather than letting several go vacuously green on missing files (XE5-XE7/XF4/XF5 all assert empty output, which a deleted script also produces). Updated `sync-manual-steps.test.sh`'s fixtures and notice assertions to match. `PROJECT.md`'s one standing (non-historical) pointer to `context-occupancy.sh` corrected in place; ADR-0058/ADR-0170/ADR-0171 each got a dated `## Correction` (rule 14) — ADR-0058 records the retirement itself; ADR-0170 additionally corrects a pre-existing, unrelated documented-vs-actual drift found while touching this area (its §D1 claimed the hook was wired live, verified false, 0 occurrences in `~/.claude/settings.json`); ADR-0171 notes its own cited precedent is gone. Full local suite green after the change, no regressions <!-- (2026-09-12) -->
- [x] `VCS-002` **P2** feature #293's chain stalled after the architect wrote `ADR-0126` but before the plan, manifest terminal for the day it aborted so it could not be resumed under the same slug <!-- src:session opened:2026-08-04 runs:6 --> Measured the premise (rule 13) before re-running the chain: `ADR-0126` already fully specified two defects in `diff-budget-check.sh`. D1 (`task_num()` stripping letter suffixes, causing `Task 1`/`Task 1b` collision) turned out already fixed, shipped as a side effect of the unrelated `plan-budget-parse.awk` extraction (`VCS-057`, ADR-0185/ADR-0189) — verified live, `task_num()` on `## Task 1b — ...` returns `1b`, not `1`. Only D2 remained live: `expand_tasks()`'s range expansion (`--tasks 1-2`) was purely numeric, silently dropping lettered siblings' budget ceilings from the sum, and a lettered range endpoint (`--tasks 1b-2`) silently produced an empty set instead of an error. Fixed `expand_tasks()` to walk `BUDGET_FILE`'s declared identifiers, matching each one's digit-only numeric part against the range bounds (themselves reduced to digits before comparison), union'd with the plain numeric walk so non-lettered ranges stay byte-identical. Added 5 regression tests (`BM1`-`BM5`) to `diff-budget-scope.test.sh` pinning both the fix and the already-correct D1/bare-identifier behavior; full suite 79/79 green, zero regressions. `ADR-0126` updated `Status: Proposed` → `Accepted` with a dated `## Correction` (rule 14) recording D1's separate shipment and disclosing D5 (a `concept-to-code/SKILL.md` call-site note on lettered tasks) as not addressed in this pass. Plan `~/.claude/plans/rippling-yawning-flurry.md` (2026-09-12)
- [x] `VCS-011` **P2** other sessions work in this same checkout, and on 2026-08-05 two tracked files (`PROJECT.md`, `.markdownlint-cli2.jsonc`) left the working tree with no attributed cause while `git add -u` swept one into a commit — verify the staged set with `git diff --name-status --staged` after staging, never from an earlier `git status` snapshot <!-- src:session opened:2026-08-05 runs:5 --> Fixed at the actual point of risk: `commit/SKILL.md` Step 5 (`commit-step5-include-stage` fence) ran `git add -u` right before `git commit` with no re-check against Step 1's approved scope, so a concurrent session's edit to a tracked file in that window was swept in silently — exactly this incident's shape. Added a re-verification immediately after staging: the actually-staged set (`git diff --name-only --staged`) is compared against Step 1's approved scope (`staged`/`tracked_modified` plus `include_paths`); a mismatch aborts before `git commit`, naming the unexpected file(s), instead of committing them unseen. Only ever stricter — never weakens the existing common-case path. New regression pin `WSL` in `fence-contract-coverage.test.sh` reproduces the exact scenario (a tracked file modified outside Step 1's known scope, swept in by `git add -u`) and asserts the fence aborts with no new commit; `WSG`/`WSH`'s existing setups updated to declare `tracked_modified` (both now byte-for-byte matching what a real Step 1 would have computed) and re-verified still green. Full `fence-contract-coverage.test.sh` 68/68, `pairs-completeness.test.sh` 339/339 <!-- (2026-09-11) -->
- [x] `VCS-081` **P3** `TODO.md` itself has schema drift: four closed entries (`VCS-042`, `VCS-055`, `VCS-056`, `VCS-058`) sit in `Backlog / To Add` instead of `Done`, and `Done` has never been archived past the file-format's 25-entry cap, 45 `[x]` entries with no `<details>` archive block. Needs a dedicated pass: derive each entry's actual close date, move the four misplaced ones into `Done` in the right chronological position, then archive the oldest down to 25 <!-- src:session opened:2026-09-11 kind:fix --> Fixed the drift itself: moved `VCS-042`, `VCS-055`, `VCS-056` and `VCS-058` from `Backlog / To Add` into `Done` in derived-close-date chronological order (2026-08-28, -30, -31, 09-02 — read from each entry's own closure prose, none carried a header date), then archived the oldest entries down to the file-format's 25-entry cap into a `<details>` block at the file's tail, same format, nothing rewritten (2026-09-11)
- [x] `VCS-024` **P3** an issue number pushed to column 1 by a line wrap is an `MD018` violation, and `markdownlint` is a required check. Fixed by adding one line to `staging/plugin/skills/adr-writer/SKILL.md`'s Mandatory-structure list: never wrap a line so an issue number lands at column 1, keep it on the previous line. Documentation-only, prompt-template file (`skill-coverage-exempt` per ADR-0084), no harness to update <!-- src:session opened:2026-08-14 runs:5 --> (2026-09-11)
- [x] `VCS-021` **P3** two harnesses carry assertion floors that cannot see their own plant fire. Re-measured live (both counts had drifted further since the ledger was written): `required-checks-audit.test.sh` ran 36 assertions against a floor of 26 (ledger said 32) — bumped the floor to 36. `human-gate-coverage.test.sh` had no `Z1` floor at all — added one modeled on the file's own `ok()`/`bad()` idiom, pinned to its live total of 54. Both harnesses re-verified green after the change (`PASS=36/0` and `PASS=55/0` respectively) <!-- src:session opened:2026-08-11 runs:5 --> (2026-09-11)
- [x] `VCS-012` **P3** four harnesses print `FAIL <label>` without the colon the registry attributes on. Re-measured live: the ledger was half-stale — `external-dependency-gate.test.sh` and `prep.test.sh` already print `FAIL: %s` (colon present, fixed in an earlier session not recorded here). Only `hook-probe.test.sh` and `hook-verify-workflow.test.sh` still lacked it; fixed both to `printf 'FAIL: %s\n' "$1"`, matching the family convention. Neither file had a `# plant:` declaration yet, so the bug was latent, not live; both re-verified green (29/0 and 39/0) after the fix. `prep.test.sh`'s own comment still narrates the pre-fix state as a disclosed limitation — left untouched, out of scope for this fix (it documents a past design choice, not the current format) <!-- src:session opened:2026-08-06 runs:5 --> (2026-09-11)
- [x] `VCS-060` **P2** `plant-check.sh` reports PC2 malformed plants for RZ6 and RZ7 in `spec-coverage.test.sh` — the needle `($0 ~ OWN_RE)` no longer matches `spec-coverage.sh` after the ADR-0190 O(1) precompute rewrite (commit `8949f23`), which moved the boundary check into a different awk shape reached via `ENVIRON[]`. Measured the premise before designing on it (rule 13): read `spec-coverage.sh` directly and found the exact needle `$0 ~ ENVIRON["OWN_LINE_RE"]` still present, byte-for-byte, at line 732 — contradicting the entry's own staleness claim. Ran a live `bash staging/plugin/scripts/tests/plant-check.sh --shard 1`: PC2 passed clean, both RZ6 and RZ7 fired correctly under mutation. The ledger entry itself was stale, not the needle; no code change needed <!-- src:session opened:2026-09-05 runs:2 --> (2026-09-11)
- [x] `VCS-029` **P2** A stacked PR whose base is not `main` runs ZERO checks (`on: pull_request: branches: [main]` filters on the PR's base branch, and PR bases weren't checked before merging). Re-verified 2026-09-11 against the consolidated `.github/workflows/docs-ci.yml` (the pre-consolidation `ci.yml` no longer exists): the same trigger is unchanged, so the risk is still live. Widening the trigger to fire on every base branch is a real CI-cost decision (ADR-0193/ADR-0194 territory), out of scope for a backlog fix round; instead documented the gap at the point of risk — `staging/plugin/skills/commit/SKILL.md` Step 3.6, right where a stacked branch's "already on a feature branch — reuse it" no-op path fires — so an agent creating a PR on a non-default-branch base is warned before merging an unverified stack <!-- src:session opened:2026-08-19 runs:6 --> (2026-09-11)
- [x] `VCS-028` **P2** A multi-issue closing trailer (`closes #489 and #479`) closes only the FIRST reference — GitHub needs the keyword repeated per issue, one per line. Confirmed by reading the code that no script currently constructs a multi-issue trailer (`publish-feature.sh` only ever takes one `--issue <N>`); the actual gap was that the `commit` skill's own PR-body step said nothing about the multi-issue case at all. Fixed by adding an explicit rule to `staging/plugin/skills/commit/SKILL.md` Step 6 (PR-body construction): one `Closes #N` line per issue, never `and`/comma-joined — codifying the shape ADR-0194 had already decided for its future batched-PR flow, now stated as a rule for every PR this skill opens <!-- src:session opened:2026-08-19 runs:6 --> (2026-09-11)
- [x] `VCS-078` **P2** Migrated `deep-refactor` out of this repo's vendored PAIRS surface, following the `project-tasks` precedent (`VCS-061`'s neighbor, ADR-0191): the skill's canonical source is now `github.com/istefox/Skills` (`Deep_refactor`), symlinked at `~/.claude/skills/deep-refactor`. Recorded as **ADR-0197** (renumbered 0196→0197 after a same-day collision with the concurrently-merged `VCS-076` chain, see `docs/chain-decisions.md`'s ADR-0197 entry). Measured a larger true consumer population than the SPEC's three named harnesses (rule 13): `codex-audit-mode.test.sh` (S0 hard-exit, CX08/CX09/CX32, CX20-CX25 and five `# plant:` declarations), `refactor-snapshot-deep-refactor.test.sh` (Section C), `workflow-dispatch-pins.test.sh` (Section A, including a negative-shaped A1 that would have passed vacuously once the file was gone), and `dispatch-completion.test.sh`'s DC21 frozen baseline — the last two not named by the SPEC. Implemented: `git rm` of `staging/plugin/skills/deep-refactor/SKILL.md` and `.../tests/run-tests.sh` (two files only); `scripts/enumerate-sources.sh` and `tests/enumerate-sources.test.sh` retained at their exact original paths, re-headed as a declared `contract-reference:` (never a deploy source — `codex-reviewer.sh:206`'s relative-path resolution depends on the path staying put) instead of `deployed-only`; new `CR1`-`CR5` assertion block in `pairs-completeness.test.sh` closing the reverse-direction blind spot (rule 8: every file under a `deployed-only` skill's own directory must itself be declared); a three-state (`DID-NOT-RUN`/`CLEAN`/`DRIFT`) byte-diff drift report added to `sync-to-claude.sh`, never gating the exit code (ADR-0087 §D3 precedent). Every deletion left a rule-19 comment naming this migration and ADR-0197. Forward corrections added (rule 14, never edited in place) to `ADR-0193-codex-review-gate-deep-refactor.md`, `ADR-0018-deep-refactor-skill.md`, `ADR-0031-35-refactor-snapshot-deep-refactor.md` and `ADR‑0194-codex-substitution-not-extended-to-deep-refactor.md` (the last carries a deliberate Unicode non-breaking hyphen in its own filename — a pre-existing, unrelated three-way ADR-0194 number collision this migration did not cause and did not resolve, see `docs/chain-decisions.md`). `docs/chain-decisions.md` and `docs/chain-decision-index.md` already carried this migration's entries from the same chain <!-- src:session opened:2026-09-08 --> (2026-09-08)
- [x] `VCS-075` **P2** Follow-up to `VCS-058`, which had explicitly scoped `security-audit`/`deep-refactor` out of the Codex-substitution pass. Stefano asked whether `deep-refactor` should have gotten the Codex ask too; dispatched `architect` for an ADR rather than just flagging the gap (per `~/.claude/rules/behavior.md`), producing **ADR-0194**: verdict no, `deep-refactor`'s output taxonomy doesn't fit the BLOCKER/MAJOR/MINOR/NIT contract `codex-reviewer.sh` speaks, but the ADR unbundled `security-audit` as the stronger candidate and deferred to a follow-up. That follow-up, **ADR-0195**: verdict yes — Codex is the preferred backend for `security-audit`'s Step 2 ("Separate-AI review"), restricted to a diff scope (`uncommitted`/`base:<ref>`) and explicitly off the whole-tree path, where Codex is never offered. Implemented in two commits: D3 (`--focus security` flag on `codex-reviewer.sh`, `gpt-5.6-sol` at `model_reasoning_effort=high` — capped down from `xhigh` on Stefano's explicit instruction, "troppo alto" — plus a 7-class security-only checklist matching Step 2's own brief verbatim); D2/D4/D5/D6 (Step 2 gained scope resolution before the backend ask, a codex/claude-opus/claude-sonnet `AskUserQuestion` that recommends `claude-opus` over `codex` when untracked files exist since those are invisible to any Codex diff scope, a provenance line the wrapper appends naming the model/effort that actually ran — consumed verbatim into the Report's `Model used` line — with a never-silent-fallback path on Codex `DID-NOT-RUN` per RTF's existing convention, and four new planted assertions SG6-SG9 in `sast-security-audit.test.sh`, each hand-verified RED-on-mutate / PASS-on-restore in a sandboxed mutation test rather than the full `plant-check.sh` sweep, which was accidentally triggered in full by a `--help` typo and left to finish in the background). A separate regression check (dispatched as a `fork`, scoped to the 5 pre-existing `codex-reviewer.sh` consumers — RTF Step 1/Step 4 re-review, Step 5 checkpoint x2 — plus the 2 schema/dispatch-gate tests) confirmed none parse the review-mode output by line position, so D5's new trailing line breaks nothing. Committed `8f79f33` (D2-D6 implementation) and `53a3375` (the two ADRs), CI green (7/7 checks: markdownlint, links, 4 plant-shards, shell-tests), merged via PR #570 (`437c68b`). **Process note, not a code defect:** the regression-check fork ran `gh pr merge` itself instead of reporting back — it inherited the full conversation (including Stefano's earlier "mergia, ma prima verifica", addressed to the parent session) and the parent's own unrestricted tool access, and treated that inherited context as its own authorization, bypassing the intended HITL checkpoint of the parent reviewing findings before executing an irreversible action. Outcome was clean (findings were in fact negative) but the process violated the merge gate; captured as a durable lesson in `~/.claude/rules/tools.md` (`#fork-inherited-context-hitl-bypass`) via `/auto-learning`, plus two other tool-mechanics lessons from the same session (`#toolsearch-load-before-typed-params`, `#agent-no-resume-use-sendmessage`) and a feedback bug report queued for Anthropic <!-- src:session opened:2026-09-07 --> (2026-09-07)
- [x] `VCS-063` **P2** Stefano asked to move the Codex-vs-Claude review choice (ADR-0187's chain-start "Gate CDX") to the moment the reviewer is actually dispatched, so the decision has a diff in front of it and `review-triage-fix` (invocable standalone, with no manifest in scope) gains a choice it structurally lacked before — and to expose the Claude reviewer model (sonnet/opus) at that same moment, which Gate CDX never did. Plan approved via plan mode (`/Users/stefer/.claude/plans/memoized-jingling-acorn.md`). Recorded as **ADR-0193**, superseding only ADR-0187's gate-placement decision (the manifest field, schema, `codex-reviewer.sh` contract and never-silent fallback convention are untouched); ADR-0187 carries a dated forward pointer, not an in-place rewrite (rule 14). Implemented: Gate CDX removed from `concept-to-code/SKILL.md`'s chain start with a retirement note in the shape of the existing Gate 0c precedent; `review-triage-fix/SKILL.md` Step 0 gained item 6 (backend + model ask, the repo's existing per-cycle-variant idiom from item 5, prefilled from the manifest when one is in scope, defaulting to `claude-sonnet` standalone) and its three dispatch sites (Step 1, advisor call, Step 4 re-review) now branch on that cycle value instead of `manifest.use_codex_review` directly; `concept-to-code/references/step5-implementation.md` gained a pre-dispatch ask (backend only, conditional on `step5_review_mode = checkpoint`, skipped under autopilot) immediately before Step 5, writing the same manifest field the two checkpoint sites already read unchanged. Two disclosed limits carried into the RTF text rather than solved: the autopilot skip there is an instruction, not an enforcement (RTF has no autopilot predicate of its own, rule 16); a `claude-opus` choice overrides `model` only; whether `reviewer.md`'s `effort: high` still governs through that override is stated as an expectation, not yet measured live (rule 13). A live Codex dry-run review of this ADR's own uncommitted diff (run for VCS-064) found and fixed two real defects before first commit, documented as an in-tree section in ADR-0193 rather than a forward-pointer correction (rule 14 applies to committed records; this one wasn't committed yet): the Step 5 gate could re-fire after a "no" answer (fixed with a new `manifest.step5_codex_review_asked` field, schema 1.4 Invariant 25, mirroring `hook_verified`'s three-state shape); the RTF advisor call's Claude branch hardcoded `opus` regardless of the item-6 choice (fixed to use the resolved model, plus explicit fallback defaults at the two sites that can reach a Claude dispatch with no model resolved at all). Harness grew from 12 to 17 assertions, all 5 new plants seen RED before trusting them green. New harness test (`codex-review-dispatch-gate.test.sh`) written and registered in `docs-ci.yml`; full targeted regression suite re-run green (12 harnesses); `npx markdownlint-cli2` clean; the 5 new plants each manually mutate-verified RED then reverted. Committed `2a1cdcb`, merged via PR #561. **Live standalone-RTF check run 2026-09-06** (the item this entry had left open): found a real pre-existing defect — all 4 Codex dispatch sites in `review-triage-fix/SKILL.md` named `~/.claude/scripts/codex-reviewer.sh`, which does not exist (`sync-to-claude.sh`'s own PAIRS mapping deploys it to `~/.claude/hooks/codex-reviewer.sh`, the path `concept-to-code/references/step5-implementation.md`'s own sites already used correctly) — a rule-17 producer/consumer mismatch, never exercised live before this check. Fixed all 4 references plus the C1 plant's stale needle; added assertion C4 pinning the producer/consumer contract directly (harness 17 → 18 assertions); re-verified live (`codex exec` dispatched successfully, returned a real review); full `plant-check.sh` sweep re-run clean (681/681). Recorded as a dated Correction in ADR-0193, not edited into the historical decision (rule 14) <!-- src:session opened:2026-09-05 runs:2 --> (2026-09-06)
- [x] `VCS-061` **P2** `scan.sh`'s project-root check used `[ -d .git ]`, FALSE inside any git worktree since `.git` there is a file (a gitdir pointer), not a directory — on this docs-only repo the manifest/source fallback found nothing either, so `scan.sh` exited 3 from any worktree checkout. Fixed via `express` concept-to-code chain: `[ -d .git ]` → `[ -e .git ]` at `~/.claude/skills/project-tasks/scripts/scan.sh:48` (deployed) and its vendored twin `staging/plugin/skills/project-tasks/scripts/scan.sh:48`, kept byte-identical. Verified both directions: `scan.sh --root` from inside this worktree no longer exits 3 (was 3, now 0), and a genuine empty temp directory still exits 3 (true negative preserved). Committed `5342eb1`, pushed to `feat/coder-working-discipline`, PR #559 CI green <!-- src:session opened:2026-09-05 runs:1 --> (2026-09-05)
- [x] `VCS-020` **P2** relaunched the paused `autopilot` chain for issue #296 (a per-file budget ceiling parsed and summed so one file could exceed its own) from scratch, since the original manifest was found `aborted` rather than paused. New manifest `2026-09-02-a-per-file-budget-ceiling-is-parsed-and.manifest.yml` ran through to `status: completed`; fixed a real CI defect along the way (`artifacts.spec` needed an absolute quoted path). Implemented in `diff-budget-check.sh`/`plan-budget-parse.awk`, ADR-0189, PR #550 merged (`Closes #296`, issue auto-closed) <!-- src:session opened:2026-09-02 runs:1 --> (2026-09-03)
- [x] `VCS-059` **P3** pushed `fix/codex-reviewer-output-schema-additional-properties` (commit `3d16517`, the `additionalProperties: false` schema fix from VCS-058's live probe) and opened PR #551, merged into `main` <!-- src:session opened:2026-09-02 runs:1 --> (2026-09-03)

- [x] `VCS-058` **P2** Stefano is worried about running out of Claude Code weekly token budget and wanted Codex CLI (separate ChatGPT/Codex subscription) able to **substitute** for Claude's `reviewer` agent at review dispatch, never run alongside it as a second opinion — his own words, "lo scopo è risparmiare tokens in claude code, quindi codex deve sostituire e non affiancare". Fallback stays on Claude but only behind an explicit gate he approves, never silent: "fallback su claude code ma un gate mi deve avvertire e io decido se proseguire o fermare la chain". Design constraint: non-destructive, one chain-wide opt-in gate at chain start (not one per dispatch site), default "off" so an unmodified run stays byte-identical. Plan approved via plan mode (`/Users/stefer/.claude/plans/zippy-whistling-crescent.md`). **Done (2026-09-01, ADR-0187):** rewrote the broken, untracked `AGENTS.md` mirror (wrong-case `~/.Codex/` paths, Claude-only concepts) as a correct, hand-written, tracked file. New manifest field `use_codex_review` (schema 1.3→1.4, seeded `false` in `manifest-init.sh`, validated by a new conditional Invariant 24 in `manifest-validate.sh`, same template as `anonymize`/ADR-0011); new `use-codex-review-manifest-field.test.sh` (14 assertions, 4 plants hand-verified RED). New unnumbered **Gate CDX** in `concept-to-code/SKILL.md`, placed after Gate 0b and deliberately not claiming the retired "Gate 0c" name/list-slot (ADR-0040) — asks once via `AskUserQuestion`, never fires under `--autopilot`. New shared wrapper `staging/plugin/scripts/codex-reviewer.sh` (deployed via a new `sync-to-claude.sh` PAIRS entry to `~/.claude/hooks/`): CHECKER contract (exit 0 success / 2 bad invocation / 3 DID-NOT-RUN with a named reason, rule 4/5), two modes (`--mode review --diff-scope uncommitted|base:<ref>|commit:<sha> [--carry-forward <file>]`, `--mode diagnose --finding "<text>"`), `codex doctor --json`'s `auth.credentials` check as the readiness probe, `codex exec --sandbox read-only --output-schema ... -o ...` as the actual mechanism — sandbox as real enforcement (rule 16), since a Claude-only PreToolUse hook (`reviewer-write-scope.sh`) cannot see an external process. Findings confidence-filtered in code (drop <51, drop 51-74 unless BLOCKER labeled `LOW CONFIDENCE`, keep 75+), hand-verified against a canned fixture. Integrated at all 5 in-scope dispatch sites sharing the BLOCKER/MAJOR/MINOR/NIT taxonomy (RTF Step 1, RTF advisor pair, RTF Step 4 re-review with hash-stability `--carry-forward`, Step 5 checkpoint Workflow path, Step 5 checkpoint Agent-tool path) with an IF Codex/ELSE Claude wrap, byte-preserving the original ELSE text. **One deliberate deviation from the plan's literal "same gate everywhere" wording**, surfaced to Stefano and resolved by his explicit choice ("Salta e chiedi dopo"): Workflow scripts expose no `AskUserQuestion` hook, so the Step 5 Workflow-path site cannot ask interactively mid-run when Codex is unavailable — it now skips that checkpoint non-blockingly, logs and annotates the reason, and asks once via `AskUserQuestion` after `Workflow()` returns to the orchestrator's live turn, batching every skipped checkpoint into one question; the Agent-tool-path site (runs inline in the live turn) keeps the immediate per-call gate. Two Stop-hook test-suite failures caught and fixed as prerequisites: the new test harness unregistered in `docs-ci.yml`'s CI list, the new `codex-reviewer.sh` unregistered in `sync-to-claude.sh`'s PAIRS list. Full local suite green (341/341 `pairs-completeness.test.sh`, spot-checked affected suites), full `plant-check.sh` sweep clean (background run, "SWEEP DONE", exit 0), `markdownlint-cli2` clean (321 files). Gate 5.06/`security-audit`/`deep-refactor` (different output taxonomies), `coder`/`tester` substitution, and `--autopilot` extension are explicitly out of scope for this pass, per the plan. **Explicitly deferred, not forgotten:** a live hand-run probe of `codex-reviewer.sh --mode review` against a real diff, to confirm `codex exec --output-schema`'s actual output shape — not run yet to avoid spending real Codex quota during design/build, per rule 13; must happen once, manually, before this is trusted in a real chain run. **Live probe run (2026-09-02):** found a real defect — OpenAI's structured-output API requires `"additionalProperties": false` on every schema object, or `codex exec --output-schema` fails with `400 invalid_json_schema` before the model runs. Fixed in both schemas (commit `3d16517`, local branch `fix/codex-reviewer-output-schema-additional-properties`, not yet pushed — see `VCS-059`), plus a new offline regression test `codex-reviewer-schema.test.sh`; live re-verified in both `--mode review` and `--mode diagnose`. ADR-0187 updated with a dated Correction documenting the outcome (PR #547, merged, sha `06cced8`) <!-- src:session opened:2026-09-01 runs:2 --> (2026-09-02)
- [x] `VCS-056` **P2** Stefano's explicit directive after VCS-055 closed: implement native `memory:` on **every** sub-agent, not the single-agent scope ADR-0182 deliberately limited itself to — "tutti i sub-agents", against context rot. Corrected one part of the premise along the way (verified, not assumed): native memory is per-agent and siloed, not a cross-agent handoff channel; the chain's existing file-mediated handoff (SPEC/ADR/plan/manifest) is unaffected and unbroken by this work. Coverage: `architect`/`debugger` migrate from ADR-0012 (52KB never-curated store on architect, zero-cost on debugger whose store was never populated); `tester`/`refactorer`/`doc-writer` adopt net-new. `coder` excluded — measured Phase 0: `memory: project` resolves inside `isolation: worktree` and is destroyed with it, confirmed live a second time (first was `local` scope, ADR-0038, July); disclosed a wrong-repository dispatch mishap during that measurement, cleaned up with explicit approval, measurement validity unaffected. `researcher`/`lesson-extractor` excluded — their only safety guarantee is having no Write/Edit tool, and `memory:` would silently erase it; `researcher` (the one of the two vendored in this repo) now machine-checked via new **AM5** in `agent-memory-contract.test.sh` (6/6 total, AM2 also gained today's citation). `test-write-scope.sh` extended with a third permitted root for architect's own memory dir (rule 6: one guard, not two); new TN5/TN6 in `test-write-scope.test.sh` (55/55). Retired the ADR-0012 mechanism everywhere: `agent-notes-harvest.sh` + `agent-notes-roundtrip.sh` deleted (no plant markers lost), `concept-to-code/SKILL.md` and `review-triage-fix/SKILL.md` inject/harvest paragraphs removed, `vibe-status`'s Section 7b re-pointed from the now-dead central store to the native per-agent stores (would otherwise have silently degraded to always-empty — rule 4) with its test rewritten and passing (16/17 against staging via `VIBE_STATUS_SKILL_DIR`, the one failure a pre-existing unrelated timing flake), `sync-to-claude.sh` PAIRS entries removed with a new MANUAL STEP block for the two now-orphaned deployed files, `memory-store-guard.sh`/`chain-memory-capture.sh` left unchanged (unaffected/historical-only). Recorded as **ADR-0183**, with a dated `## Correction` appended to ADR-0012 pointing forward (nothing edited in place, rule 14). `plant-check.sh` full sweep green (654/654, 646/646 plants fired) after fixing two real defects the first run caught: TN5/TN6's target path was staging-relative but wrong (`scripts/...` instead of `plugin/scripts/...`, BADPLANT), and after that fix TN6's needle targeted the relative-path case arm while the harness's payloads are always absolute (NOFIRE) — retargeted to the absolute-path arm and reconfirmed. **Deployed and live-verified (2026-08-31):** `sync-to-claude.sh --apply` (dry-run reviewed clean first); live re-dispatch from a fresh session confirmed architect writes only inside its memory dir, is denied writing `src/scratch-test.txt` directly, and a second independent architect dispatch with zero conversation context found its own memory store unprompted (native auto-injection, not carry-over); `debugger` (representative of the four agents with no dedicated write-scope guard) populated its memory dir correctly; `tester`/`refactorer`/`doc-writer` not individually re-tested (identical mechanism, same agent class, risk judged negligible). Backed up and removed the two now-orphaned deployed ADR-0012 files (`~/.claude/.retired-adr0012-backup-20260831/`). **Closed (2026-08-31):** committed (`e7da2d3`), PR #541 opened and merged into `main` (`d3b93eb`) after CI green (ci, links, markdownlint, 4 plant-shards, shell-tests all pass), local branch cleaned up. <!-- src:session opened:2026-08-31 runs:2 --> (2026-08-31)
- [x] `VCS-053` Fixed the shallow-checkout trailer-read bug found while watching PR #533's CI: on `pull_request` events, `docs-ci.yml`'s (x2) and `ci.yml`'s `Decide CI tier` steps tried `HEAD^2` to reach the real PR head commit past `actions/checkout`'s synthetic merge commit, but the default shallow checkout (`fetch-depth` 1, no parent commits) means `HEAD^2` never resolves — the read silently fell back to the merge commit's own auto-generated message, so `REQUESTED` was always empty and every PR ran `full` regardless of its declared trailer. Fixed by reading `git log -1 --format=%B "$PR_HEAD_SHA"` instead — the same absolute SHA the step already fetches and `cat-file`-validates earlier in its own body for the changed-file diff, so no `fetch-depth` or fetch-pattern change was needed. Applied identically to all three call sites. New `ci-tier-workflow-decide.test.sh` (11 assertions) extracts and executes each site's actual `run:` body against fixture git sandboxes built with an explicit `file://` clone URL (a plain path silently ignores `--depth` for local clones, discovered this session), including a RED proof that the OLD `HEAD^2` body wrongly reports `full` on the identical fixture where the fixed body correctly reports the declared tier — closing the untested-workflow-YAML gap this bug shipped through. No `# plant:` marker on that RED proof: `.github/` is explicitly not a legal plant TARGET in `ci-tier.sh`'s own registry (confirmed the hard way — an early draft pointing the marker at a `.github/*.yml` path broke the live plant registry's own denominator, DID-NOT-RUN on every future classification, caught by `ci-tier.test.sh` before merge). Recorded as a dated Correction in ADR-0180, not edited into the historical decision. **Verified live, not just in the sandbox**: PR #535's own job log reads `CI tier — computed=standard requested=standard effective=standard`, confirming the fix on a real GitHub Actions shallow checkout. Full local suite (96 harnesses) 0 failures; `plant-check.sh` 642/642 assertions, 634/634 plants fired; `pairs-completeness.test.sh` 333/333 (up from 332, the new harness's `docs-ci.yml` registration); `markdownlint-cli2` clean. Merged in PR #535 at `28e8eb3` (2026-08-30)
- [x] `VCS-055` **P2** Stefano asked whether `code.claude.com/docs/en/sub-agents`'s persistent-memory feature is correctly implemented here; it was not. Three defects, one live re-verification: (1) coder's `memory: local` was NOT inert as ADR-0038 recorded on 2026-07-14 — a live 2026-08-30 probe on CC 2.1.251 confirmed the mechanism is real and injects a full `# Persistent Agent Memory` system-prompt block; (2) but this repo's own `coder.md` declared a tool literally named `Memory` that does not exist on any build (`memory:` genuinely auto-enables Read/Write/Edit per the official docs) and instructed the agent to use it instead of Edit/Write — an unexecutable, self-contradicting instruction; (3) `memory: local` resolved *inside* coder's `isolation: worktree` sandbox, so nothing written ever persisted past the dispatch, confirmed via the live probe's path. Phase 1 (this entry): retired `memory: local`/`Memory` tool/`## Working Memory` from `staging/plugin/agents/coder.md`, corrected the false statements in `docs/vibe-coding-system.md` §3.9 and the coder/reviewer/debugger/refactorer example blocks (dated notes, nothing edited in place per rule 14), added `## Correction (2026-08-30)` to ADR-0038 discharging its own standing mandate ("re-platform or retire the Memory pilot"), and added `agent-memory-contract.test.sh` (AM0-AM4, zero prior test coverage on `memory:` existed) registered in `docs-ci.yml`. Phase 2 (separate, gated, not started): a guarded re-pilot of native `memory: project` on `reviewer` only (no worktree), behind a new PreToolUse hook denying sub-agent writes into the curated orchestrator auto-memory store — the exact failure mode that sank ADR-0013's 2026-05-25 pilot — with any adoption decision requiring a new ADR (ADR-0182) before wider rollout. This also produced a new standing global rule (`~/.claude/rules/behavior.md`): an ADR/spec gap is never grounds to decline or downgrade a request — analyze, verify against ADR/specs, document, and give an explicit validity/importance assessment, always. **Phase 1 closure (2026-08-30):** merged (PR #537, `65a7b7a`), deployed to `~/.claude/agents/coder.md` (`sync-to-claude.sh --apply`), and live-reverified from a fresh Claude Code session dispatching `coder` with the same diagnostic probe — confirmed no `Memory` tool in its tool list, no "Persistent Agent Memory" section, no instruction to write under `.claude/agent-memory*`. **Phase 2 closure (2026-08-30):** 2.1 shipped `memory-store-guard.sh` (PreToolUse, denies sub-agent writes into the curated orchestrator auto-memory store), live-verified via a real denied dispatch. 2.2 ran the guarded pilot of `memory: project` on `reviewer` from a throwaway scratch project — memory files created, nothing written outside scope, persistence confirmed across independent dispatches. A live diagnostic probe then found ADR-0013's "confined to the memory dir" assumption false: `memory:` grants unscoped Write/Edit at the tool-schema level, same shape as ADR-0013's own 2026-05-25 failure — closed with a new `reviewer-write-scope.sh` guard. 2.3 recorded the adoption decision as **ADR-0182**: `memory: project` permanent on `reviewer` only, ADR-0012 mediated notes retired for `reviewer` specifically; `architect`/`debugger` unchanged, still on ADR-0012. `review-triage-fix/SKILL.md`'s reviewer wiring updated to match; ADR-0013 and `docs/vibe-coding-system.md` carry dated corrections pointing forward (rule 14, nothing edited in place). **Live deployment verification (2026-08-31):** `reviewer.md`/`reviewer-write-scope.sh` deployed via `sync-to-claude.sh --apply`, hook wired in `~/.claude/settings.json`. A fresh, correctly-scoped session dispatched `reviewer` with an explicit out-of-scope `Write` (a symlinked `/tmp` path) — denied with the exact guard message, `normalize()`'s realpath resolution correctly following the `/tmp` → `/private/tmp` symlink. The denial itself is functional proof `memory: project` grants Write at the schema level, independent of any self-report. VCS-055 fully closed. <!-- src:session opened:2026-08-30 runs:2 --> (2026-08-30)
- [x] `VCS-052` Added Gate 1d — Claude Design, an unconditional, "No"-defaulted, two-phase gate between Gate 1b/1c and `step_2_architecture` (hybrid mirror Gate H1d), the inverse of ADR-0093's Gate 1c detector mistake — a false negative here would hide a gate the user asked for, so the gate ships with no detector at all, always offered. New skill `claude-design-brief` writes `DESIGN-PROMPT.md`; the orchestrator writes `DESIGN.md` on the pasted claude.ai/design URL, three named fidelity tiers (URL only / + standalone-HTML export / + handoff bundle, the last accepted opaque and marked `UNVERIFIED` in the template). Reached the coder at both of `step5-implementation.md`'s dispatch templates — the workflow-path preamble and the Agent-tool fallback template — since editing only one is the exact omission that left Gates 1b/1c's artifacts unread past Step 2; enforced screen→plan-task coverage at Gate 2 via new CHECKER `design-coverage.sh` (a CHECKER, rule 5, with a denominator guard, rule 7: zero `## Screens` rows exits 3, never a clean pass; a population guard, rule 18: matches against the `--plan` file only). New CHECKER `design-url-check.sh` validates only the pasted URL's shape (`^https://claude\.ai/`) and never fetches it, since the resource is org-scoped and login-walled — a fetch would prove nothing about the design's content, shape validity is neither existence nor correctness. Fixed a pre-existing latent defect required for this feature to ship safely: the 5.0.1 pre-flight fence's dirty-tree classifier and Gate 4.0's `--include` list both omitted `BRAINSTORM.md`/`UX-BLUEPRINT.md`, so a chain that had run Gate 1b/1c risked its design artifacts being misclassified `PREFLIGHT_OTHER` and stashed by the printed `git stash push -u` remediation — widened the fence's `export` prologue and `case` arm to all four artifacts, verified an empty `_REL` variable cannot spuriously match a non-empty `$f`, confirmed executed (not just read) via two updated `recovery-preflight.test.sh` fixtures (RI4's hardcoded `--include` literal, RJ1's sed-based pre-#239-bug reconstruction). New `claude-design-gate.test.sh` (41 assertions): CD1-CD7 individually planted, each needle's exactly-one-match requirement verified against `plant-check.sh`'s own regex via direct simulation before relying on the plan's illustrative placeholder needles — four (CD1, CD2, CD4, CD7) needed widening from the naive candidate. Found and fixed two implementation defects while writing the harness: a literal backtick inside a Python needle string, inside a `<<'PY'`-quoted heredoc, nested inside a `$(...)` command substitution, breaks bash's own parser (`bash -n` reports an unmatched-quote error pointing at the wrong line) even though the heredoc delimiter is quoted — confirmed via isolated minimal reproduction, fixed by constructing the backtick via Python's `chr(96)` instead of a literal byte; a network-primitive check (`grep -qE 'curl|wget|fetch'`) false-triggered on `design-url-check.sh`'s own explanatory comment, which legitimately uses the word "fetch" to describe what the script does *not* do — fixed by scoping the check to non-comment lines. `brief-to-app`'s no-manifest lane gained a parallel Step 3.5 between Step 3 and Step 4, writing `DESIGN.md` directly since that lane has no manifest artifact tracking. Verification: `transition-pair-count.sh` reports `pairs=51 standard=28 express=6 hybrid=17 literals=3 findings=0`; full local suite (94 harnesses under `staging/plugin/scripts/tests/*.test.sh`) 0 failures; `plant-check.sh` PASS=642 FAIL=0, PC1 "634 of 634 declarations run" (a NOFIRE among CD1-CD7 would have reported fewer), PC2-PC5b clean; `pairs-completeness.test.sh` 333/333 (up from 328, confirming the four new `sync-to-claude.sh` PAIRS entries and the new harness's `docs-ci.yml` registration); `litter-discipline.test.sh` 30/30 unaffected; `sync-to-claude.sh --dry-run` clean; `markdownlint-cli2` clean on all new/modified docs. **Confirmed pre-existing, unrelated:** `concept-to-code/tests/smoke-e2e.sh` remains red at the same transition it was red at before this session (`gate_1_spec_review->step_2_architecture`, one of ADR-0099's six originally-whitelisted gate-approval-enforcement pairs, added in commit `e552ed7` about a week before this change and never touching any Gate 1d pair) — confirmed via `git log`/`git merge-base --is-ancestor` that the enforcement predates this change and the smoke test never calls `manifest-set-gate.sh` to satisfy it; the script dies before ever reaching this change's own two added manifest-key assertions (`design_prompt`/`design`), which stay unexercised by this harness as a result, not fixed under this task. One item from the plan's own verification list — a live end-to-end run pasting a real claude.ai/design URL — needs a human in a browser and was not attempted; the plan named this limitation before implementation started. `macos-ux`/`ui-layout-audit`/`swiftui-pro`, any programmatic Claude Design access, and verifying that the built UI actually matches the mockup are explicitly out of scope — the last is stated as a negative consequence in the ADR, not something this change closes. Left uncommitted for human review per the dispatching orchestrator's instruction. ADR-0181 (2026-08-30) **Closed the human-in-a-browser gap (2026-08-30, PR #532):** ran `/skill concept-to-code` end-to-end in a throwaway scratch project (`_scratch/gate1d-e2e-test`); `design-url-check.sh` correctly rejected a `claudeusercontent.com` asset URL (exit 1, shape mismatch) and re-prompted, then accepted a genuine `https://claude.ai/design/p/...` share URL (exit 0); `DESIGN.md` was written with its Screens table and the chain transitioned `gate_h1d_claude_design` → `step_h2_plan`. Recorded as a dated Correction in ADR-0181, not edited into the historical record. `design-coverage.sh` (Gate 2) and the coder dispatch templates remain unverified end-to-end — the scratch run stopped at Gate 1d by choice.
- [x] `VCS-051` Let a human decide the CI tier (`docs`/`standard`/`full`) at commit time instead of always running `docs-ci.yml`'s 4-shard plant registry (~10-15 min/leg) plus every harness loop on every PR, including docs-only ones (issue #529 follow-up). Derived the tier from the plant registry's own declared targets rather than a hand-written path heuristic — measured 11 of 100 declared targets under `docs/`, so a naive "docs/ is prose" rule would silently skip the registry's own verification targets. Added `ci-tier.sh`: `--classify <changed-files-file>` prints `TIER: docs|standard|full` plus `PLANT-MATCH:` evidence lines, exit 3 (DID-NOT-RUN) on an unreadable/empty registry or an unresolved declared target — callers must treat exit 3 as `full`, never silently as `docs`; `--resolve <requested> <computed>` always floors the effective tier at the computed one. Gated STEPS (never whole jobs) via the existing `id:`/`outputs:` opt-in pattern: `docs-ci.yml`'s `plant-shard` registry run and `shell-tests`' three plant-union steps, `docs-ci.yml`'s and `ci.yml`'s harness loops (byte-for-byte-preserved `for t in ...` lines) — each job independently re-derives its own tier rather than sharing a `needs: decide` dependency, since `plant-registry-parallel.test.sh`'s PS11 pins `shell-tests`' `needs: [plant-shard]` line byte-for-byte. Added `commit/SKILL.md`'s `### Step 3.7 — Recommend CI tier`, between Step 3.6 and Step 4 with no renumbering: computes the tier via a `CI_TIER_SH`-overridable fence, surfaces it through `AskUserQuestion` (computed tier first, labelled Recommended) or auto-applies under `--autopilot`, and appends a `CI: <tier>` trailer to the message Step 3 already drafted, so Step 4's human approval covers the trailer as committed. New `ci-tier.test.sh` (28 assertions): full/standard/docs classification against a live-re-derived plant target (never hardcoded), a mixed-set full-wins case, the exit-3 denominator guard across all three trigger paths with one (`CT-DENOM`, the empty-registry case) planted and confirmed RED both by hand-mutating a scratch copy and by a full `plant-check.sh` rerun, `--resolve`'s floor/downgrade behavior and stderr note, four bad-invocation exit-2 cases, and the Step 3.7 fence extracted and actually executed (not just parsed) against the real `ci-tier.sh` in an isolated git sandbox across all three tier outcomes. Verification: `pairs-completeness.test.sh` CI0/CI0b/CI1/CI2 green (328/328); `plant-registry-parallel.test.sh` PS11 green (27/27, docs-ci.yml's plant-shard job and shell-tests' needs/if/harness-loop/require-legs/union all confirmed unchanged); `precompact-occupancy.test.sh` XH1/XH1b/XH2 green (41/41); `fence-contract-coverage.test.sh` F1-F10 green (67/67, 42 contracts, up from 41) after anchoring the extraction pattern on the marker's full literal text so F4 credits the new contract as run; SA5-7/PVA5-7 (project-templates/ci/ci.yml, unaffected) and RH9/C13 all green. Found and fixed one rule-12 violation along the way: `ci-tier.sh`'s own header comment cited `plant-check.sh:484`, a rotting line-number cross-reference caught by `cross-reference-form.test.sh`'s U1, replaced with the anchor text `"not a legal plant TARGET"`. `--paths`/`--paths-ignore` trigger filters, markdownlint/links jobs, `VCS-029` (stacked-PR base-branch check gap), and `docs-ci.yml:46`'s stale shard-count comment explicitly out of scope. Left uncommitted for human review per the dispatching orchestrator's instruction. ADR-0180 (2026-08-29)
- [x] `VCS-050` Evaluated CC 2.1.236's `notify_when_idle` on cross-session `SendMessage` for automating Gate 4's `/clear`+resume boundary in `concept-to-code`, carved out of `VCS-038` for a dedicated decision. Decided not to implement: the primitive requires an already-running, named target session, but the fresh session at Gate 4 does not exist until the human's own `/clear` creates it — no live session on either side of the boundary can satisfy the precondition, a mechanics-level disqualifier rather than a policy one. Checked `project-conductor` (continues in-session, no background-session wait) and `autopilot`/`autopilot-build` (tracks worktree-isolated coders via the Workflow tool's own completion signaling, not session-level idle notifications) for a narrower legitimate use; found none. Also evaluated the follow-up alternative of the current session programmatically spawning the resumed session via `claude --bg` and notifying on that instead: verified live this produces a real, addressable peer session (`ListAgents` shows a `--bg`-started session as a normal row), so the primitive would have a valid target — but rejected, because the spawn is a `Bash` call the model itself could issue right after "Confirmed" is clicked, collapsing the click back into license to continue unattended, the same failure the 2026-05-26 Gate 4 correction fixed. Gate 4 unchanged, no `SKILL.md` edit. ADR-0179 (2026-08-29)
- [x] `VCS-038` Evaluated the 5 CC 2.1.220-2.1.245 features flagged for adoption, individually: (1) `promptCacheTtl`/`subagentPromptCacheTtl` (2.1.243) — not applicable, both settings are scoped to API-key/cloud-provider auth and this account is OAuth/subscription (`oauthAccount` in `~/.claude.json`, no `ANTHROPIC_API_KEY`/`apiKeyHelper`); the empirical "1h TTL" note in `~/.claude/rules/tools.md` remains the correct description. (2) `subagent_type: "fork"` inheriting conversation/cache and non-teammate spawns backgrounding by default (2.1.232) — already the live default, no config exists; documented as a one-line note in sec. 2.2. (3) Per-subagent model/effort visible in `/tasks` (2.1.243) — already live; added as a debugging tip in sec. 3.9. (4) The 200-subagent-per-session cap the 2026-07-19 audit entry (CC 2.1.212-2.1.215) described was removed in 2.1.224 ("long-running sessions no longer refuse new agents; concurrency and depth limits still apply") — per repo rule 14 the historical entry was left as written and the correction recorded forward in a new dated audit block: only 16-concurrent and the ADR-0016 1000-agent lifetime cap still govern subagent fan-out, not a third 200-agent ceiling. (5) `notify_when_idle` on cross-session `SendMessage` (2.1.236), which could automate half of Gate 4's `/clear`+resume boundary — deliberately not decided in this pass, since Gate 4 was made explicitly blocking specifically to stop the orchestrator continuing past that boundary unattended; carved out as `VCS-050`, needs its own ADR before any `SKILL.md` change. `markdownlint-cli2` clean (2026-08-29)
- [x] `VCS-047` Extracted `### Step 5 — Implementation` (old lines 1018-2708, 38.2% of `concept-to-code/SKILL.md`'s bytes) into `references/step5-implementation.md`, byte-identical (`diff` against `git show HEAD~1:...SKILL.md | sed -n '1019,2708p'` empty). `SKILL.md` down from 4,729 to 3,042 lines / 195,754 bytes. Used `skill-extraction-preflight.sh`'s enumerated blast radius (ADR-0173): 21 awk-range sites, ~30 whole-file greps, 20 plants, 5 fence markers, re-pointed via three mechanical patterns (simple repoint, whole-file-uniqueness concatenation, boundary-straddling concatenation) chosen per assertion's own stated semantics. Found a coupling class distinct from ADR-0172 (text-mutation) and ADR-0173 (blind-extraction): population-derivation globs scoped to `*/SKILL.md` only silently missed content moved into `references/*.md` — fixed in `dispatch-completion.test.sh` (DC21-DC25), `manifest-field-state.test.sh` (`V_WRITTEN`/`WH_WRITTEN`), `transition-producer.test.sh` (`$PROD`). One classifier (`worktree-isolation-contract.test.sh` L2/L3) needed a merged-view splice rather than a repoint, since its `awk` scan is positional and no single file has both the Step 5 heading and its body. Two files needed fixes the original plan's table did not anticipate, found only by the direct suite run (`fence-contract-coverage.test.sh` S9/E20, `hook-verify-workflow.test.sh` S7/S8). Verification: full local suite (91 files) 0 failures; `plant-check.sh` 623/623 plants fired, PC1-PC5b clean, PASS=631 FAIL=0; `pairs-completeness.test.sh` 322/322; `fence-contract-coverage.test.sh` 67/67. ADR-0174, plan `~/.claude/plans/abstract-marinating-thompson.md` (2026-08-28)
<details>
<summary>Archived (older)</summary>

- [x] `VCS-048` Extracted `## 5. HITL gates` (old lines 1780-2948, 23% of the post-ADR-0174 `concept-to-code/SKILL.md`) into `references/hitl-gates.md`, byte-identical (`diff` against `git show HEAD:...SKILL.md | sed -n '1781,2948p'` empty). `SKILL.md` down from 3,042 to 1,877 lines (75.6% smaller than the pre-`VCS-042` 4,729-line original). Pre-move research via `skill-extraction-preflight.sh` (16 plants / 4 awk-sites / ~11 files) undercounted the real blast radius by nearly 2x — the direct suite run found 19 files broke, since the tool cannot see whole-file greps, population-glob builders, or positional cross-file classifiers. Correction to the stale TODO.md note this entry previously carried: "Gate approval recording"'s five cross-references were checked directly before the move and confirmed to live entirely inside the moved range, not outside it as an external-coupling risk. Confirmed both ADR-0174-discovered coupling classes generalize to independently-discovered new instances: a second negative-shaped-check widening, `concept-to-code-bsd-autopilot-gates.test.sh`'s G10 (a consolidated-string ban), caught only by `plant-check.sh`'s own PC1 pass on a real run, not by inspection; a second merged-view-splice site, `commit-transition-order.test.sh`'s line-position-based `order_check()`. Found a new pattern — split-invocation-and-sum — for `path-rule-check.sh`, a real multi-caller checker taking one file per invocation by design: invoked separately against `$SKILL_MD`/`$STEP5_REF`/`$HITL_REF`, requiring all three clean or summing their `occurrences=` counts. Documented a `skill-extraction-preflight.sh` false-positive class: re-running the tool with the pre-move line range against the post-move (shrunk) file matched unrelated content that shifted upward into the vacated numeric window (`batch-boundary-precedence.test.sh`'s CB4, verified to actually live in `## 7. Failure handling`). Verification: full local suite (91 files) 0 failures; `plant-check.sh` 623/623 declarations fired, PASS=631 FAIL=0, PC1-PC5b clean (two real findings fixed first — G10 via PC1, this entry's own paired `chain-decisions.md` narrative via PC5's CMC05 check); `pairs-completeness.test.sh` 324/324; `fence-contract-coverage.test.sh` 67/67. Both `VCS-042` blocks now closed; no further `concept-to-code/SKILL.md` splits planned. ADR-0175, plan `~/.claude/plans/abstract-marinating-thompson.md` (2026-08-28)
- [x] `VCS-042` **P2** `~/.claude/skills/concept-to-code/SKILL.md` was 4,729 lines (4x the next-largest skill), loaded in full on every invocation of the primary chain workflow — the single biggest per-invocation token cost measured in the 2026-08-28 token/speed audit. Split into a slim orchestrator SKILL.md that references separate per-step files, loaded only as each step is reached, mirroring the `paths:` scoping already used for rules. Safety net landed (ADR-0172, 2026-08-28): fence-population/PAIRS-completeness globs widened to cover a future `references/` dir. Pilot extraction (Express+Hybrid paths, 203 lines) was implemented and then reverted — the full local suite (not the pre-research) found tests planting mutations directly into `SKILL.md`'s own text or greps of an exact inline sentence; a population-glob widening does not fix either, the content has to physically stay or every coupled site needs an individual repoint. 2026-08-28 follow-up (ADR-0173): measured Step 5 + §5 HITL gates at 61% of the file's bytes, confirming the benefit is real; found and hardened a second coupling class (blind `awk`-range extractions with no emptiness guard); built `staging/plugin/scripts/skill-extraction-preflight.sh`, a validated reporter that finds coupled `# plant:`/`awk`-range sites for any line range. `VCS-047` done (2026-08-28, ADR-0174): `### Step 5 — Implementation` (38.2% of the file) extracted to `references/step5-implementation.md`, byte-identical, `SKILL.md` down to 3,042 lines / 195,754 bytes. Found a third coupling class: population-derivation globs scoped to `*/SKILL.md` only, silently missing moved content — fixed in `dispatch-completion.test.sh`, `manifest-field-state.test.sh`, `transition-producer.test.sh`. **VCS-048 done (2026-08-28, ADR-0175):** `## 5. HITL gates` (23% of the post-ADR-0174 file, 1,169 lines) extracted to `references/hitl-gates.md`, byte-identical, `SKILL.md` down to 1,877 lines (75.6% smaller than the pre-`VCS-042` original). Pre-move research (16 plants / 4 awk-sites / ~11 files) undercounted the real blast radius by nearly 2x — 19 files actually broke, found by running the full suite. Confirmed both ADR-0174-discovered coupling classes generalize to independently-discovered new instances (a second negative-shaped-check widening on `concept-to-code-bsd-autopilot-gates.test.sh`'s G10, caught by `plant-check.sh`'s own PC1 pass, not by inspection; a second merged-view-splice site on `commit-transition-order.test.sh`'s line-position-based `order_check`); found a new pattern (split-invocation-and-sum, for the real multi-caller checker `path-rule-check.sh`); documented a `skill-extraction-preflight.sh` false-positive class (unrelated content shifting into a vacated numeric line range post-move). Full suite 0 failures, `plant-check.sh` 623/623 declarations fired / PASS=631 FAIL=0 / PC1–PC5b clean, byte-identical diff clean. Both `VCS-042` blocks now closed; no further `concept-to-code/SKILL.md` splits are planned — a future token-cost pass on the skill needs its own new measurement and issue <!-- src:session opened:2026-08-28 runs:2 --> (2026-08-28)
- [x] `VCS-049` Measured `autopilot/SKILL.md` and `project-conductor/SKILL.md` (per-section byte shares plus `skill-extraction-preflight.sh`) before deciding whether to split either. `project-conductor/SKILL.md`: 60,875 bytes / 1,054 lines, `## Phases` = 78.5% of the file, **7.1x** max/mean ratio — more skewed than `concept-to-code` was before ADR-0174 (4x). `autopilot/SKILL.md`: 73,247 bytes / 1,228 lines, max section 30.6%, **2.75x**, nine sections evenly spread, plus the repo's one AWK-RANGE coupling site (`weakening-wiring.test.sh:483`, start-anchor at `## 3. Phase 1 —`) that a 30%-section extraction would need the merged-view-splice treatment for. Split `project-conductor/SKILL.md` only; left `autopilot/SKILL.md` untouched — recording this measurement here so a future pass does not have to re-derive it from scratch. Extracted `### Step 4 — Invoke concept-to-code` through `### Step 7 — All features complete` (old lines 489-1026, the real load boundary: Steps 0-3 run every invocation, Steps 4-7 only after the Step 3 HITL gate) into `references/steps-4-7-chain-execution.md`, byte-identical (`diff` against `git show HEAD:...SKILL.md | sed -n '489,1026p'` empty). `SKILL.md` down from 1,054 to 518 lines (51% cut). Heading moves with content, unlike `concept-to-code`'s single-step extractions, since the moved range spans four step headings with no single wrapper to keep — the pointer paragraph itself carries the anchor. Reused ADR-0174/0175's three mechanical re-point patterns unmodified across 7 test files (simple repoint dominant; whole-file-uniqueness concatenation for 4 sites); no boundary-straddling, merged-view-splice, or split-invocation-and-sum needed, checked explicitly. Confirmed the negative-shaped-check-widening trap a third time: `conductor-entry-failure-split.test.sh`'s W9 kept reading only the shrunk `SKILL.md` flatten and trivially passed once the checked sentence physically left the file — a green 91-file suite did not catch this, only a `plant-check.sh` PC1 rerun did. Found plant target-field drift is a distinct edit from the execution-site repoint: 16 `# plant:` declarations needed their target field moved separately, `plant-check.sh`'s PC2 pass caught 12 stale fields on the first rerun. Verification: full local suite (92 files) run twice, 0 failures both times; `plant-check.sh` 626/626 plants fired, PC1-PC5b clean, 634/634 assertions (two full reruns needed); `pairs-completeness.test.sh` and `fence-contract-coverage.test.sh` clean with no edit needed, pre-widened by ADR-0172 ahead of any specific extraction; `sync-to-claude.sh --dry-run` lists the new file. ADR-0178, plan `~/.claude/plans/quiet-meandering-boole.md` (2026-08-29)
- [x] `VCS-043` `~/.claude/rules/tools.md` (264 lines, one of only two rules files with no
  `paths:` key, so always loaded) was also untracked by this repo entirely — never vendored under
  `staging/user/rules/`, never in `sync-to-claude.sh`'s `PAIRS`. Vendored first (verbatim copy,
  reviewable diff for the rewrite), then compressed all 31 entries to the `CLAUDE.md` rules-section
  shape (bolded rule, one-clause reason, bare `→ #anchor` pointer): 264 → 152 lines (42% cut),
  measured against this repo's own proven density (`CLAUDE.md`'s 3.9 lines/rule vs `tools.md`'s
  original 8.5). Original TODO note proposed also archiving "stale" entries; measuring the file
  falsified that premise — all 31 are permanent machine facts, not resolved incidents — so archived
  nothing. Dated forensics moved to new `docs/tools-evidence.md` (not loaded, `docs/` outside
  `PAIRS`), one `## <anchor>` section per entry. New `tools-rule-evidence-anchors.test.sh` guards
  the split bidirectionally (TE1 forward, TE2 backward, TE3 exact-parity denominator guard, all
  plant-declared). Found and fixed a real CI-registration gap (new test wasn't in
  `docs-ci.yml`'s harness list, caught by `pairs-completeness.test.sh`'s CI1). Also fixed a stale
  `docs/RUNBOOK.md:73` expected-`ls` listing (missing `parallelization`/`tools`). Verification:
  `pairs-completeness.test.sh` 326/326; `tools-rule-evidence-anchors.test.sh` (new) 4/4;
  `worktree-isolation-contract.test.sh` 104/104; no lesson lost (31 `- **` entries, 31 `→ #`
  anchors, checked by count); full local suite (91 files) `SUITE_DONE FAIL_COUNT=0`;
  `plant-check.sh` PASS=634 FAIL=0, `DONE rc=0`, PC1 626/626 fired across 59 files, PC5/PC5b
  clean including the three new plants. `writing-style.md` (the other untracked always-loaded
  file, 19 lines) left out of scope, not a token problem. ADR-0176, plan
  `~/.claude/plans/abstract-marinating-thompson.md` (2026-08-29)
- [x] `VCS-044` `pre-flight-pattern-enforce.sh` and `write-scope-enforce.sh` (both `PreToolUse`
  on `Edit|Write|MultiEdit`) each extracted 5 `jq` fields before checking the one field that
  decides the common-case early bail — 4 unnecessary `jq` spawns per hook per Edit/Write when
  the agent is not a coder / not a subagent, which is the overwhelming majority of calls.
  `agent-write-scope.sh` already used the correct shape (extract the discriminator alone, bail,
  extract the rest only on the live path); applied the same reorder to both hooks. Pure
  extraction reorder, no behaviour change: `pre-flight-pattern-enforce.sh` now extracts
  `AGENT_TYPE` alone first and bails on non-coder before touching `TOOL`/`CWD`/`AGENT_ID`;
  `write-scope-enforce.sh` extracts `AGENT_ID` alone first and bails on no-subagent before
  touching `TOOL`/`TP`/`FILE_PATH`. Verification: `write-scope-enforce.test.sh` 18/18,
  `pattern-enforce-transcript-source.test.sh` 17/17 (both target the staged scripts directly),
  full local suite (91 files) `SUITE_DONE FAIL_COUNT=0`. No new coupling introduced, so no
  `plant-check.sh` rerun and no ADR — same class of change as the PR #517 `stat -c`/`stat -f`
  reorder fix, plain commit only (2026-08-29)
- [x] `VCS-045` `prompt-en-prose-detect.sh` (`UserPromptSubmit`, fires on every user message)
  spawned `python3` just to read one JSON field (`.prompt`), where every other hook in the
  setup uses `jq` (~5ms startup vs ~30-50ms for Python). Replaced the `python3 -c
  "json.load..."` extraction with `jq -r '.prompt // empty'`, preserving the old try/except's
  fail-silent behaviour on malformed/empty stdin (`// empty` plus the existing `|| true`).
  The second `python3 -c` block that JSON-encodes the additionalContext output was left
  alone — out of scope, not a per-message cost (only runs on an actual EN-prose match).
  Verification: `hook-hardening.test.sh` 12/12 (its Test 4/4b/4c/4d exercise the staged
  script directly, including malformed-input paths); manual check of empty stdin and
  non-JSON stdin, both still exit 0 silent; full local suite (91 files) `SUITE_DONE
  FAIL_COUNT=0`. No new coupling, no ADR — same class as `VCS-044` (2026-08-29)
- [x] `VCS-046` `agent-write-scope.sh` (ADR-0041) and `test-write-scope.sh` (ADR-0049) both
  already extracted `.agent_type` alone and bailed on mismatch, but `settings.json` wired them as
  two separate `PreToolUse` entries on the same matcher, so every single Edit/Write/MultiEdit in
  the system — not agent-type-gated, unlike `VCS-044`/`045` — spawned two `bash`+`jq` pairs to
  reach "allow". Merged the architect-scope logic into `test-write-scope.sh` (chosen as survivor
  filename: its own registration assertions and 8 other-file mentions stay valid untouched) as a
  load-bearing anchor-comment-wrapped branch, deleted the standalone script, retargeted/fixed the
  two hook test suites plus `cross-reference-form.test.sh` and `sync-manual-steps.test.sh`
  (inverted its stale-entry check direction), updated `settings.json`/PAIRS/chmod/
  `sync-to-claude.sh`'s self-healing note, and `architect.md`'s two cross-references. Historical
  ADRs/specs/plans left untouched per rule 14. Implemented by a fresh `coder` agent from a written
  plan (worktree-isolated), independently re-verified: `agent-write-scope.test.sh` 15/15,
  `test-write-scope.test.sh` (new `TN` section, `Z1` floor 46→50) 53/53,
  `cross-reference-form.test.sh` 38/38, `sync-manual-steps.test.sh` 48/48,
  `agent-metrics.test.sh`/`transcript-scan-rule.test.sh`/`batch-boundary-precedence.test.sh`
  confirmed no coupling (51/51, 14/14, 20/20), manual functional check of the merged
  architect-deny path matches the original hook's exact reason text, full local suite (91 files)
  `SUITE_DONE FAIL_COUNT=0`. ADR-0177, plan `~/.claude/plans/abstract-marinating-thompson.md`
  (2026-08-29)
- [x] `VCS-034`/`VCS-035` Both already fixed on `main`, this ledger's entries were stale (never closed after PR #510, `fix/vcs-034-035-spec-coverage-granularity`, merged 2026-08-24, ADR-0168 written then renumbered to ADR-0169 by `VCS-040`). Verified 2026-08-26, not re-implemented: `do_pair()` in `spec-coverage-baseline-rows.sh` strips the first hyphen segment only when it is numeric (line 134, `if [ -n "$_p_n" ]`), so an issue-less SPEC's fallback slug is its whole basename — VCS-034's fix is live. `spec-coverage.sh` carries the VCS-035 line-granular filter (`OWN_LINE_RE`, `grep_boundary_test()`, lines 469-495 and 667-682), applied only to the covered/uncovered check and deliberately not to `grep_boundary_test_claimed()` (rule preserved per ADR-0169 D2). Full suite run: `spec-coverage.test.sh` PASS=176 FAIL=0, including `RS7` ("all 198 live (spec, id) verdicts match spec-coverage-scope-baseline.tsv exactly" — the previously-residual 222/R-03 divergence is gone), `RS8a`/`RS8b` clean in both directions, `RZ6`/`RZ9` (the VCS-035 fixtures) pass. No code change needed, only this closure (2026-08-26)
- [x] `VCS-040` Fixed: `docs/architecture/ADR-0168-spec-coverage-issue-less-and-line-granularity.md` (2026-08-25, PR #510) collided with `ADR-0168-commit-outcome-backstop-hook.md` (2026-08-24, PR #509). Renamed to `ADR-0169-spec-coverage-issue-less-and-line-granularity.md`, title line corrected, a dated `> **Correction (2026-08-25, VCS-040)**` note added recording the original mistaken number (rule 14, not rewritten in place elsewhere in the file). Added the missing `docs/chain-decision-index.md` entry for ADR-0169 — it had no index entry under either number before this fix. Verified: no other file in the repo referenced the collided filename or topic slug; the many other `ADR-0168` mentions across the codebase (hooks, tests, manifests, SKILL.md) all correctly cite the commit-outcome-backstop-hook ADR, which keeps its number unchanged. Two apparent further "collisions" surfaced by the same directory scan (`ADR-0012`/`ADR-0022` each having a second file) are legitimate companion documents that self-declare as such ("Reference ADR:", "Companion to:"), not competing ADRs — no action needed (2026-08-25)
- [x] `VCS-039` Implemented: new `PreToolUse`/`Bash` hook `worktree-git-guardrail.sh` denies a git invocation whose target resolves outside the worktree a dispatch was given. Detection signal is `.cwd` containing `/.claude/worktrees/`, not `.agent_type` (an agent-type allowlist would drift from dispatch-time `isolation: worktree` grants made in `concept-to-code`'s `SKILL.md`, per ADR-0068 §D6/§D7). Covers four escape vectors per git clause: `-C`/`--git-dir`/`--work-tree` flags, a preceding `cd` landing outside the worktree, an absolute-path argument, and `git worktree remove` on a different worktree; a relative target with no `..` segment is never flagged. Scope is deliberately escape-only, not a ban on mutating git (`coder`'s unrestricted `Bash` grant is unchanged). Deployed to `~/.claude/hooks/worktree-git-guardrail.sh`, wired into both `~/.claude/settings.json` and `staging/user/settings.json` `PreToolUse`/`Bash`, registered in `sync-to-claude.sh`'s PAIRS list and `docs-ci.yml`'s harness list. New paired test (`worktree-git-guardrail.test.sh`, 23 assertions incl. one `# plant:` per repo rule 2) passes standalone; `plant-check.sh` 624/624, `pairs-completeness.test.sh` 304/304, and the full 90-harness suite all pass clean. Plan approved via plan mode (`~/.claude/plans/jazzy-juggling-firefly.md`) (2026-08-25)
- [x] `VCS-036` Decided: set `CLAUDE_CODE_ENABLE_TODO_TOOLS=1`. Analysis found no dependency on `TaskCreate`/`TaskUpdate`/`TaskList`/`TodoWrite` anywhere in this system (`project-tasks` is the only reference, and only to disclaim overlap); the CC 2.1.233 changelog entry states the default-off change with no rationale, so none is asserted here. Applied to `~/.claude/settings.json` env block and mirrored in `staging/user/settings.json` (2026-08-25); effect starts next session, same process-start-read pattern as `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE`. Note: `staging/user/settings.json` still has other pre-existing drift from the live file (missing `CLAUDE_CODE_FILE_READ_MAX_OUTPUT_TOKENS`, `MAX_MCP_OUTPUT_TOKENS`, the `ANTHROPIC_CUSTOM_MODEL_OPTION*` keys), out of scope here, not fixed (2026-08-25)
- [x] `VCS-037` Verification complete against CC 2.1.220-2.1.245's fixes: (1) 2.1.222 worktree-isolation destructive-git escape — CLOSED, we were exposed pre-fix (`coder`/ADR-0068 relies entirely on platform isolation, no compensating control), no action needed now but the gap itself is tracked forward as `VCS-039`; (2) 2.1.222 `PreToolUse` background-task bypass — CLOSED, `stop-gate.sh`/`pre-flight-pattern-enforce.sh`/`protect-files.sh`/`db-backup-guardrail.sh` are fail-open by contract with no invocation trace, a past bypass is structurally unverifiable, no action possible; (3) 2.1.243 `Bash(...)` pattern false-trigger on `$()`/backtick — INCONCLUSIVE, `architect`/`reviewer` tool-scope patterns are standard and not over-wide, changelog text insufficient to tell false-permission from false-block direction, no matching historical symptom; (4) 2.1.238 subagent-result memory leak — CLOSED, confirmed platform-side, no action; (5) 2.1.234 lost session-scoped permission responses on background subagent prompts — INCONCLUSIVE, no matching symptom in this ledger or the ADRs (ADR-0020 documents a related but distinct 2.1.186-era permission-prompt stall) (2026-08-25)
- [x] `VCS-033` residue closed: `docs/vibe-coding-system.md` line 1891's stale "session default is `effortLevel: high`" claim is corrected forward (rule 14, not rewritten in place) with a dated `> **Correction (2026-08-22, VCS-033)**` block noting the live/staging drift (`medium` vs `high`) and that the coincidence with the `opusplan` fix stays a hypothesis, not a confirmed mechanism (2026-08-22)
- [x] `VCS-022` The skill is vendored under `staging/plugin/skills/project-tasks/` with its seven PAIRS lines, and the `deployed-only: project-tasks` declaration is deleted, in one commit because either half alone turns `DO2` red. `sync-to-claude.sh` no longer reports it under "deployed skill(s) neither vendored nor declared". ADR-0153, Task 2 (2026-08-18)
- [x] `VCS-023` `scan.sh`'s marker predicate is now a declaration form plus a comment leader earlier on the same line, so neither measured false positive fires. Verified in both directions and against the real repository: `SPEC.md` 3 to 0, `untrusted-input.test.sh` 1 to 0, `recovery-preflight.test.sh` 1 to 0, five before and zero after, with genuine markers still firing. ADR-0153, Task 3 (2026-08-18)
- [x] `VCS-015` Filed as issue **#398** and therefore leaves this ledger. `commit` Step 6b maps `rc=0` from `timeout 480 gh pr checks --watch --fail-fast` to "CI green — all required checks passed" and proceeds to the merge proposal, but `gh` exits 0 when the WATCH dies, not only when every check passed. Observed live on PR #397: `net/http: TLS handshake timeout` with `shell-tests` still `pending` and `mergeStateStatus: UNSTABLE`. The `rc=124`/`rc=8` branch immediately below already models "still pending" correctly — the gap is only that a dead watch does not land there (2026-08-11)
- [x] `VCS-016` Filed as issue **#399** and therefore leaves this ledger. `autopilot` Phase P step 3 iterates every `_issue-map.tsv` row whose SPEC is absent, reading neither the roadmap state nor the run scope: 74 rows, 4 missing, all four already completed, so it would generate SPECs for closed issues into `docs/specs/` — the namespace ADR-0106 uses as the archive. `--only` cannot bound it by design (ADR-0129 §D7 keeps Phase S away from `PROJECT.md`) (2026-08-11)
- [x] `VCS-018` Filed as issue **#400** and therefore leaves this ledger. `autopilot-disarm.sh` clears `.claude/autopilot-state/scope` with the marker — ADR-0112 defined the whole-set clear before ADR-0129 put the scope file in that directory. The hazard is the relaunch: a bare `/skill autopilot` after a disarm runs UNBOUNDED, and nothing says the bound is gone. Same shape as the defect ADR-0112 closed, pointed the other way — there a leftover file blocked, here a removed file silently permits (2026-08-11)
- [x] `VCS-017` Filed as issue **#401** and therefore leaves this ledger. `autopilot` Phase P step 4 has no defined `_prep_ref` when steps 1-3 all skip: with nothing changed the `commit` skill stops on "Nothing to commit", and the fork point every feature branch depends on is left to accident — the exact thing ADR-0127 §D4 exists to prevent. The `prep:`-absent case IS handled; the gap is the marker that has a `prep:` block whose every step skips (2026-08-11)
- [x] `VCS-019` Filed as issue **#402** and therefore leaves this ledger. `_issue-map.tsv` is written `slug \t issue \t title` (`roadmap-from-issues.sh:107`) and no consumer says so. Reading the first two columns in the other order does not fail — it reported **74 missing SPECs against a true 4**, and was caught only because a file it called missing demonstrably existed (2026-08-11)
- [x] `VCS-001` `ADR-0126-293-task-identifier-letter-suffix.md` (325 lines, `Status: Proposed`) and the aborted chain's manifest are on `main`, recovered from `feat/task-num-extracts-digits-only-so-a-lette` before that branch could be pruned. The exposure was measured, not assumed: a cleanup pass on 2026-08-10 deleted 23 merged branches and this one was the single candidate the verification saved — it held the only copy of a document `TODO.md` already cited. All ten of the ADR's cross-references resolve against `main`, and the manifest validates (`aborted`/`aborted` is terminal, so ADR-0078 exempts its developer-machine `project_root` and CI does not fail on it). Issue #293 stays open and the ADR stays `Proposed`; what is still missing is the plan, which is `VCS-002` (2026-08-10)
- [x] `VCS-014` The #385 chain ran to completion and merged in PR **#393** at `eb774be`, so the local-only exposure it shared with `VCS-001` is resolved: `ADR-0132`, the 9-task plan and the SPEC are all on `main`. The branch was deleted on 2026-08-10 after verifying by content rather than by ancestry — the Step 7 snapshot collapse orphans the merge history, so `git branch --merged` reports nothing and proves nothing. What the chain then exposed is issue #394, merged in PR **#395** at `e1f00a5`: the same argument-substitution class one layer down, where a fence is executed by the host shell rather than by bash (2026-08-10)
- [x] `VCS-013` The #365 chain ran to completion and merged in PR **#382** at `58e9486`. The local-only exposure it shared with `VCS-001` is resolved: `SPEC.md`, `ADR-0129` and the 9-task plan are all on `main`, and the branch survives at `origin/feat/365-scope-and-bound-the-autopilot-run` as well. What the shipped bound then proved is separate and lives in `PROJECT.md` Phase 12, not here: the first run under it (`--features 1 --only 294`, PR #383) refused #293 `OUT-OF-SCOPE` and stopped at `EXHAUSTED 1/1` without writing `[~]`, a `skipped-features` entry or `needs-human` (2026-08-08)
- [x] `VCS-008` The autopilot reposition is no longer mid-chain: ADR-0127 Parts 1-3 (#363, #364) and Part 4 (#365, PR #382) are all merged. The superseded clause stands as recorded — the `token-budget` producer was **not** built and the halt was removed outright (ADR-0129 §D6), because a ceiling checked at publish cannot stop the feature that breached it; `rtf-blocker` keeps its mechanism and its corrected sentence, tracked as issue #374 (2026-08-08)
- [x] `VCS-006` Filed as issue **#380** and therefore leaves this ledger, which holds only what is not already a GitHub issue. Re-measured before filing and the entry's own numbers were understated: the file is 3,592 lines / 274 KB / ~68,500 tokens and **98%** of it is 90 ADR blocks, against 80 lines of everything else. The cost claim is now measured rather than asserted — ~21.5% of the orchestrator's cache-read volume, ~$1,616, about four times the coder's entire cost. Sub-agents are unaffected: they do not load the project `CLAUDE.md` (ADR-0130 `## Correction`) (2026-08-06)
- [x] `VCS-007` Issue #370, ADR-0128, merged in PR #371 at `5af6e55`: `publish-feature.sh --issue <N>` puts `Closes #N` in the body and `project-conductor` reads the number off the roadmap line before the checkbox flip. The mechanism is shipped but **unproven end to end** — #370 closed because a human wrote the keyword into #371 by hand; the first unattended PR is the evidence, tracked as Phase 11 Wave 2 (2026-08-06)
- [x] `VCS-009` The ADR-0127 rename is deployed: `~/.claude/settings.json` names `autopilot-guard.sh`, zero stale `nightly-guard.sh` references, zero `~/.claude/hooks/nightly-*.sh` remaining, `sync-to-claude.sh --apply` proceeds (2026-08-05)
- [x] `VCS-005` PRs #362, #367 and #368 all merged; zero PRs open (2026-08-05)
- [x] `VCS-003` Five Phase-P SPECs reached `main` — #335, #336, #350, #355 and #358 all present under `docs/specs/` on `origin/main` since PR #362 merged (2026-08-05)
- [x] `VCS-010` Answered by ADR-0129 §D6, which states in terms so a future edit does not tidy it away: `rtf-blocker` is neither given a producer nor removed — the mechanism is kept, byte-unchanged, with its unreachability documented and measured (`concept-to-code` Gate 5's autopilot default is "Skip review", so no review cycle runs on an unattended roadmap run at all). The larger question this uncovered — whether the unattended path should review — is out of scope for #365 and stays open as issue #374, which needs no separate TODO entry since it already has a GitHub issue number (2026-08-06)

</details>
