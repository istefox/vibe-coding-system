<!-- project-tasks: prefix=VCS lastId=48 -->
# PROJECT TASKS

Updated: 2026-08-28 · Open: 24 (P1: 0) · GitHub issues: 70

This ledger is **bilateral**: every open GitHub issue appears in the `GitHub Issues` section,
regenerated from `gh` on each run, and the sections below it hold what has no issue number yet.

That inverts the rule this header carried until 2026-08-18, which said an item with an issue number
belonged there and not here. The old rule kept the file short and made it answer half a question:
"what is open" had two answers in two places and neither was complete. Nothing is duplicated,
because the GitHub-owned fields are regenerated rather than copied, and a roadmap item carries a
pointer to its `PROJECT.md` phase rather than a transcription of it.

## GitHub Issues
- [ ] `#273` The Step 5 checkpoint has no mechanism to tell an expected red from a real one <!-- src:github state:OPEN labels:prep -->
- [ ] `#293` task_num extracts digits only so a lettered task collides with its sibling <!-- src:github state:OPEN labels:prep -->
- [ ] `#295` MALFORMED is dropped silently by any caller filtering BUDGET or SCOPE <!-- src:github state:OPEN labels:prep -->
- [ ] `#296` a per-file budget ceiling is parsed and then summed so one file can exceed its own <!-- src:github state:OPEN labels:prep -->
- [ ] `#297` the dirty-classify fence reads porcelain v1 so a renamed artifact classifies as OTHER <!-- src:github state:OPEN labels:prep -->
- [ ] `#298` the collapsed snapshot commits live in the reflog only and a git gc destroys them <!-- src:github state:OPEN labels:prep -->
- [ ] `#299` fence_is_abort_capable is lexical and four measured fences abort from outside it <!-- src:github state:OPEN labels:prep -->
- [ ] `#301` the transcript-scan population stops at two roots <!-- src:github state:OPEN labels:prep -->
- [ ] `#302` the grant-coverage guard matches a bare Bash token so an unrestricted grant in another form passes <!-- src:github state:OPEN labels:prep -->
- [ ] `#303` the interpreter enumeration is a fixed list and the escapes are pinned nowhere <!-- src:github state:OPEN labels:prep -->
- [ ] `#304` the reason floor is duplicated in seven derived guards and nothing stops one drifting <!-- src:github state:OPEN labels:prep -->
- [ ] `#306` the deployed-only registry's reasons are true today and checked by nothing <!-- src:github state:OPEN labels:prep -->
- [ ] `#307` hooks.json is outside PAIRS completeness by a deferred decision <!-- src:github state:OPEN labels:prep -->
- [ ] `#308` thirteen skill-private test runners exercise the deployed copy and are dark to CI <!-- src:github state:OPEN labels:prep -->
- [ ] `#309` settings.json is never synced and five guardrail hooks are wired by hand <!-- src:github state:OPEN labels:prep -->
- [ ] `#315` seventeen ADRs carry the same instruction-not-an-enforcement paragraph and the class is undecided <!-- src:github state:OPEN labels:prep -->
- [ ] `#335` a manifest terminal by status is exempt from invariant 4 and the state machine still lets it transition <!-- src:github state:OPEN labels:prep -->
- [ ] `#336` set-branch-protection.sh unions one context while the audit derives three <!-- src:github state:OPEN labels:prep -->
- [ ] `#374` No unattended run performs a review: Gate 5's autopilot default is Skip <!-- src:github state:OPEN labels: -->
- [ ] `#375` An unmarked leftover SPEC.md is mishandled in both directions: unknown routes brownfield, and the archive fence halts <!-- src:github state:OPEN labels: -->
- [ ] `#376` Step 1's slug stamp skips itself on any SPEC that mentions the marker in prose (rule 12) <!-- src:github state:OPEN labels: -->
- [ ] `#378` usage-report.py says sub-agent token cost is not observable, reads it anyway, and mixes it into the daily total unlabelled <!-- src:github state:OPEN labels: -->
- [ ] `#388` detect-macos.sh: a framework name in prose trips Gate 1c inside a non-UI document <!-- src:github state:OPEN labels: -->
- [ ] `#389` Five of six skippable-gate branch points have a discriminator the manifest never records <!-- src:github state:OPEN labels: -->
- [ ] `#390` The manifest's Gate 4 answer and its transition to ready_for_implementation live only in the working tree <!-- src:github state:OPEN labels: -->
- [ ] `#391` The merge-back escape check reads untracked files only, so a dispatch that ran without isolation is invisible <!-- src:github state:OPEN labels: -->
- [ ] `#398` commit Step 6b reads a dead CI watch as green: gh exits 0 when the watch dies, not only when every check passed <!-- src:github state:OPEN labels:bug -->
- [ ] `#400` autopilot-disarm clears the run scope, so a relaunch after a pause is silently unbounded <!-- src:github state:OPEN labels:chain-blocker -->
- [ ] `#401` autopilot Phase P step 4: no defined prep ref when every prep step skips <!-- src:github state:OPEN labels:chain-blocker -->
- [ ] `#402` _issue-map.tsv column order is documented in no consumer, and reading it wrong yields a plausible wrong answer <!-- src:github state:OPEN labels:documentation -->
- [ ] `#406` commit Step 5's fence cannot commit any message containing an apostrophe <!-- src:github state:OPEN labels:bug -->
- [ ] `#407` Gate 0d's git auto-detect can never take Outcome A or B <!-- src:github state:OPEN labels:bug -->
- [ ] `#413` ADR-0104 squash-merge premise is false: this repo merges with merge commits <!-- src:github state:OPEN labels:documentation -->
- [ ] `#414` Phase P step 3 and project-conductor use different predicates for SPEC coverage <!-- src:github state:OPEN labels:bug -->
- [ ] `#415` Phase 0 check 9 file-wide --only matcher aborts on issues whose number appears in a PROJECT.md heading <!-- src:github state:OPEN labels:chain-blocker -->
- [ ] `#417` The tester brief specifies what to assert and never what a good assertion is <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#418` Context is instrumented and is never a decision: the chain names a phase boundary exactly once <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#419` Budget: measures diff lines, and the axis that actually ends runs is context <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#420` The chain has no wayfinding phase: nothing gets an idea from loose to formulable <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#421` No entry point chooses between concept-to-code, autopilot and wayfinding <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#422` Express E4 and Hybrid H5 never commit the chain manifest at all <!-- src:github state:OPEN labels:bug -->
- [ ] `#426` settings.json drift runs in BOTH directions: the deployed and staged copies each register a `Stop` hook the other lacks, and `mark-dirty.sh` keeps producing markers for a consumer that was never wired <!-- src:github state:OPEN labels:prep -->
- [ ] `#427` Three items left open by #404: the .git worktree gate, an unasserted STOP_GATE_TEST_TIMEOUT=0, and an exclusion list that now covers tracked content <!-- src:github state:OPEN labels:prep -->
- [ ] `#428` run-hook-tests.sh asserts over the retired backup-before-deploy.sh, and no gate executes the file so the red has gone unobserved <!-- src:github state:OPEN labels:prep -->
- [ ] `#432` permission-mode-state.sh returns UNOBSERVABLE after any /clear and blames the build, so autopilot Phase M refuses a correctly configured machine <!-- src:github state:OPEN labels:chain-blocker -->
- [ ] `#436` The overwrite invariant lost its only backstop: 2.1.228 lets newer models Write over an unread file, and protect-files.sh is a secrets path denylist, not a diff gate <!-- src:github state:OPEN labels:bug -->
- [ ] `#437` Hand-rolled deployment is the common cause behind #426, #309 and VCS-022: staging/plugin is already a plugin, and 2.1.224's archive source with SHA-256 pinning would make staged and deployed one artifact <!-- src:github state:OPEN labels:enhancement,prep -->
- [ ] `#439` The chain discards the conversation and never runs the product, which is why /goal lands closer: a single-session lane with executable acceptance <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#440` PROJECT.md is silent about 44% of open work: the roadmap grows by audit, so 27 open issues have no phase and nothing runs the list backwards <!-- src:github state:OPEN labels:documentation -->
- [ ] `#445` F4 accepts a marker mention as proof a fence was executed, and cannot tell an extractor from a grep <!-- src:github state:OPEN labels: -->
- [ ] `#454` spec_topic_match has three values and the code has two: an unverifiable SPEC is treated as owned <!-- src:github state:OPEN labels: -->
- [ ] `#457` An in-flight manifest turns CI red between Gate 4.0 and Step 7, and no ADR names the window <!-- src:github state:OPEN labels: -->
- [ ] `#458` A red harness loop skips the union steps, so the registry's coverage check has never run in CI <!-- src:github state:OPEN labels: -->
- [ ] `#459` The chain-history event log records shell tokens as manifest states, in 6 of 61 records <!-- src:github state:OPEN labels: -->
- [ ] `#460` Step 7.0b enrols a SPEC in the scope-baseline corpus and nothing bumps the baseline <!-- src:github state:OPEN labels: -->
- [ ] `#461` The autopilot routing pre-flight tests for a SPEC.md file, and gate0-detect.sh can disown the file it finds <!-- src:github state:OPEN labels: -->
- [ ] `#462` autopilot check 9 normalises every PROJECT.md line, so a table row collides with a 40-character feature slug <!-- src:github state:OPEN labels: -->
- [ ] `#463` No autopilot pre-flight check asserts the per-feature SPECs ADR-0022 requires actually exist <!-- src:github state:OPEN labels: -->
- [ ] `#464` gate0-detect.sh detects ADRs by one naming convention, so an adr-tools repository reports adr=no <!-- src:github state:OPEN labels: -->
- [ ] `#465` Two divergent derivations of the same feature slug, and the branch, manifest and publish names depend on which wins <!-- src:github state:OPEN labels: -->
- [ ] `#466` detect-macos.sh strips tables and code spans, so a macOS SPEC that names its platform only there reports NOT_MACOS <!-- src:github state:OPEN labels: -->
- [ ] `#469` Nothing gitignores .claude/autopilot-state/, so a run's transient state lands in the PR it opens <!-- src:github state:OPEN labels: -->
- [ ] `#474` An autopilot run with no prep: block forks every feature from the previous feature's tip <!-- src:github state:OPEN labels:bug,chain-blocker -->
- [ ] `#475` assigned-secret: the prose false positive is a keyword followed by an absolute path, and it occurs 0 times in 423 files <!-- src:github state:OPEN labels:bug -->
- [ ] `#480` The conductor's slug and the SPEC's stamped slug are derived from different inputs, and disagreement is read as a disowned SPEC <!-- src:github state:OPEN labels: -->
- [ ] `#481` agent-metrics.sh's test_count_delta counts test files, not tests, so it reports 0 for every change to an existing harness <!-- src:github state:OPEN labels: -->
- [ ] `#482` spec-coverage.sh's rc=2 and diff-budget-check.sh's CLEAN both make a wrong CWD indistinguishable from a passed gate <!-- src:github state:OPEN labels: -->
- [ ] `#483` weakening-scan's zero-assertion-test fires on every pytest.raises test: the assertion-token list has no raises idiom <!-- src:github state:OPEN labels: -->
- [ ] `#484` No launcher for the arming ritual: five preconditions checked by hand, each able to fail the run late <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#485` ADR-0154 §D7's residual is not narrowable by filtering: ignoring fixture-borne ids costs 0 and removes 0 <!-- src:github state:OPEN labels: -->
## Open Issues

- [ ] `VCS-002` **P2** feature #293 must have its chain re-run from Step 2 — the architect stalled after writing the ADR and before the plan, and the manifest is terminal for today's slug so it cannot be restarted under the same name today <!-- src:session opened:2026-08-04 runs:3 -->
- [ ] `VCS-004` **P3** the autopilot run report is gitignored, so a run's spend and per-feature metrics survive only on the machine that produced them — `.claude/autopilot-report.json` (renamed from `nightly-report.json` by ADR-0127; a `file-missing` STALE record under the old name is that rename, not a closure) — now also carried as PROJECT.md Phase 11 Wave 5, where Wave 1's budget recalibration depends on it <!-- src:session opened:2026-08-04 runs:3 -->
- [ ] `VCS-011` **P2** other sessions work in this same checkout, and on 2026-08-05 two tracked files (`PROJECT.md`, `.markdownlint-cli2.jsonc`) left the working tree with no attributed cause while `git add -u` swept one into a commit — verify the staged set with `git diff --name-status --staged` after staging, never from an earlier `git status` snapshot <!-- src:session opened:2026-08-05 runs:3 -->
- [ ] `VCS-012` **P3** four harnesses print `FAIL <label>` without the colon the registry attributes on — `external-dependency-gate`, `hook-probe`, `hook-verify-workflow`, `prep` — so a plant declared in any of them is unattributable; ADR-0128 §D4 makes that a loud `BADPLANT` instead of a false "the assertion pins nothing", but the four are not converted and a green plant run says nothing about assertions in them — `staging/plugin/scripts/tests/plant-check.sh`. **Confirmed live 2026-08-19**, no longer theoretical: two plants were declared on `prep.test.sh` (PT1, PT2), both had to be withdrawn because its `no()` prints `FAIL <label>` with no colon and no id, so `plant-check.sh`'s `red_re()` (`^FAIL: <id>`) can never attribute them. The refusal is documented in the file and the two assertions were verified by hand instead — which is precisely the "a green plant run says nothing about assertions in them" state above, now with an instance <!-- src:session opened:2026-08-06 runs:3 -->
- [ ] `VCS-021` **P3** two harnesses carry assertion floors that cannot see their own plant fire — `required-checks-audit.test.sh` runs 32 assertions against a floor of 26, and `human-gate-coverage.test.sh` has no `Z1` floor at all despite gaining `HIB0b`. ADR-0124's class: a floor with slack absorbs its own plant, so removing an assertion leaves it green and the assertion pins nothing — `staging/plugin/scripts/tests/required-checks-audit.test.sh`, `staging/plugin/scripts/tests/human-gate-coverage.test.sh` <!-- src:session opened:2026-08-11 runs:3 -->
- [ ] `VCS-024` **P3** an issue number pushed to column 1 by a line wrap is an `MD018` violation, and `markdownlint` is a required check — an ADR citing issues in flowing prose fails CI for a reason that has nothing to do with its content. Observed on `ADR-0138`, twice in one file (`#404` and `#287`), caught only after the PR was already open; the fix is to rewrap so the number stays on the previous line. It will recur in every ADR that cites issue numbers, and neither the ADR template nor `adr-writer` says so. Cheapest durable form is one line in the ADR authoring guidance, not a lint-config change — `MD018` is right to flag it <!-- src:session opened:2026-08-14 runs:3 -->
- [ ] `VCS-025` **P3** the #312 plan states `staging/plugin/scripts/tests/` is PAIRS-covered "except for four named legacy files"; measured 2026-08-14 there are **six** entries under `plugin/scripts/tests/` in `sync-to-claude.sh` (`phase1.test.sh`, `prep.test.sh`, `hook-verify-workflow.test.sh`, `db-backup-guardrail.sh`, `pre-flight-pattern-enforce.sh`, `run-hook-tests.sh`). The conclusion the plan drew is unaffected — a new `.tsv` there still needs no entry, confirmed by a green `pairs-completeness.test.sh` — so the plan is NOT corrected in place (rule 14, it is a historical record). Recorded here because the correction otherwise lives only in `.claude/step5-report.json` and `.claude/autopilot-report.json`, both gitignored per `VCS-004`, and the "four" figure will be copied forward by the next plan that quotes it <!-- src:session opened:2026-08-14 runs:3 -->
- [ ] `VCS-026` **P3** verifying a post-collapse worktree branch by patch identity gives a confident WRONG answer, and it is the answer that blocks cleanup. Measured 2026-08-14 on the 17 `worktree-agent-*` branches left by the #312 run: `git cherry main <branch>` reported **17 of 17 with 100% unique patches** and no tip was an ancestor of `main`, because Step 7's snapshot collapse (ADR-0104) rewrites the commits and so changes every patch-id. The blob-level check inverted it — of ~660 files per branch, **0 to 2** blobs were absent from the 4921 reachable from `main`, and every one was an intermediate draft of six paths whose final version is in `main`. Both `git branch --merged` and `git cherry` answer "is this commit in main", which after a collapse is not the question; the question is "is this content in main". Use `git rev-list --objects main` versus `git ls-tree -r <branch>`. Recorded because the wrong tool is the one a person reaches for first, and it argues for cleanup being skipped indefinitely — related to issue #298. **Update 2026-08-19: the cleanup happened, and this entry's own prescription was not followed.** All 18 `worktree-agent-*` branches plus the four merged feature branches were deleted, local and remote, after `gh` confirmed every feature branch's PR was MERGED. But the branch-level decision used `git cherry` (which reported 1 to 7 unique patch-ids on 10 of 18, exactly the collapse artefact this entry names) plus a heuristic on commit subjects — every unique commit carried a mechanical `chore(step5): snapshot` subject and none carried human work. The blob check this entry prescribes was run only AFTERWARDS, against the recorded shas: **11 blobs absent from `main` across 5 paths** (`spec-coverage.test.sh` x5, `PROJECT.md` x3, `stop-gate-path-predicate.test.sh`, `batch-boundary-precedence.test.sh`, `ADR-0155`), and all 5 paths are present in `main` in their final version, so the 11 are intermediate drafts and nothing was lost. The prediction held; the method did not get reached for at the moment it mattered. What is still missing is anything that surfaces this entry at deletion time — the shas are in the session scratchpad only <!-- src:session opened:2026-08-14 runs:3 -->
- [ ] `VCS-027` **P2** POINTER ONLY, deliberately not a copy: the `brief-to-app` build lane is issue **#439** and its three waves are `PROJECT.md` Phase 13. Recorded here against this ledger's own rule — which says an item with an issue number leaves the file — because the operator asked for a pointer and the alternative was duplicating the issue body, which is the second source of truth ADR-0024 forbids. Nothing but the two references belongs on this line; read #439 for the problem and the plan, Phase 13 for the wave order. Delete this line when Phase 13's last wave closes <!-- src:session opened:2026-08-15 runs:3 -->
- [ ] `VCS-028` **P2** a multi-issue closing trailer closes only the FIRST reference: `closes #489 and #479` closed #489 and left #479 open. Measured 2026-08-19 — four issues (#479, #494, #478, #411) stayed open after #497/#496 merged and were closed by hand, each with a comment naming the cause. GitHub needs the keyword repeated per issue (`closes #489, closes #479`). Nothing in this repo's commit guidance says so, and `VCS-007` covers only `publish-feature.sh`'s single-issue `Closes #N`. The cost is the exact failure `PROJECT.md`'s standing rule exists for: a roadmap that disagrees with GitHub, with nothing making them agree <!-- src:session opened:2026-08-19 runs:3 -->
- [ ] `VCS-029` **P2** a stacked PR whose base is not `main` runs ZERO checks — both workflows declare `on: pull_request: branches: [main]`, and that filter is on the BASE branch. Observed 2026-08-19 on #497 (base `feat/spec-coverage-scope-back-reference`): `gh pr checks` reported "no checks reported on the branch". It reads as visibly nothing-ran rather than a false green, and branch protection still guards `main`, so the danger is bounded — but the PR is mergeable into its own base with no verification at all. Worked around by merging the stacked PR into its base first, so the base's own PR carried all 14 commits through one real CI gate. Nothing states that rule anywhere — `.github/workflows/ci.yml`, `docs-ci.yml` <!-- src:session opened:2026-08-19 runs:3 -->
- [ ] `VCS-030` **P3** `.claude/test-timeout` is 600s against a suite measured at **415s over 85 harnesses** on an unloaded machine — 1.45x, thinner than the 2x this file has carried before (159s -> 300s). Set deliberately by the operator on 2026-08-19 over the convention's ~830s, to cap a hung suite at 10 minutes rather than 14. The risk is stated rather than discovered: 415s was measured with nothing else running, and concurrent agent builds (the contention ADR-0159 addressed) grow it. The value has already fallen below its own suite once, by growth and not by error — ADR-0156 flagged it at 79 harnesses when there were 85. If a timeout reappears, re-measure before raising; ADR-0137's `TMO_MAX` caps any value at 900 <!-- src:session opened:2026-08-19 runs:3 -->
- [ ] `VCS-031` **P2** `ledger-merge.sh`'s roadmap phase pointer is uniformly WRONG on this repo, not absent — it reads a level-two heading pattern for the heading above a row, but `PROJECT.md`'s 24 phase headings are `### Phase N — …` and its only level-two headings are `Overview`, two `Where this stands` blocks and `Phases`. Measured 2026-08-19 by replaying the awk: **203 of 203 rows resolve to the literal string `Phases`**, the container, and 0 to a phase number, so every GitHub Issues line would gain `— Phases`, which is the section they are all in and therefore says nothing while looking like information. Rule 18's shape: the scan is satisfied by a population larger than the one it meant. Worked around by omitting `--roadmap`, which degrades with a stated notice as the contract promises. The fix is one pattern (`/^#{2,3} /` or `/^### Phase /`), but it needs a denominator guard too (rule 7): a pointer map that resolves 0 rows to a phase must report that, not emit a label — `~/.claude/skills/project-tasks/scripts/ledger-merge.sh` line 135, and its vendored twin `staging/plugin/skills/project-tasks/scripts/ledger-merge.sh` <!-- src:session opened:2026-08-19 runs:3 -->

- [ ] `VCS-032` **P3** The autocompact threshold TRACKS `autoCompactWindow`, which was asserted twice before it was measured. Session of 2026-08-21, `compactMetadata.preTokens` on every `compact_boundary` record: under a 300k window, 18 compactions between 07:00:19 and 08:14:22 UTC — the first at 140,753, then a flat **83,898-89,988** seventeen times in 74 minutes, which Claude Code itself reported as thrashing. The window was raised to 600k at 08:22:20 UTC; the single compaction since, at 08:54:02, reads **179,409**. Doubling the window moved the trigger 2.11x, so the key governs and the earlier "it does not" reading was wrong. What is NOT established is the function: 179,409/600k and ~85,000/300k are both ~29%, but the first compaction of the day (140,753 against 300k, 47%) does not fit, so the ratio is an observation over one sample per regime and must not be stated as a mechanism — ADR-0162 §D4 says the same thing at the enforcement point. The method error is the entry: the value was changed twice on a reading the data did not support, and the readout cost one command. The remaining lever is the ~50k fixed preamble re-created after each compaction (`cache_creation_input_tokens` 48,456-54,945 on the first turn after, 546-2,008 after that), of which the ADR index in `CLAUDE.md` was ~5.9k per turn — moved out to `docs/chain-decision-index.md` on 2026-08-21 by ADR-0163, taking `CLAUDE.md` from 35,715 to 12,368 bytes, while ADR-0162 stopped the `SessionStart` hook re-injecting the project context at every compaction. Neither is yet measured against a fresh session's first-turn `cache_creation_input_tokens`, which is the only number that closes this entry. Method note for the next reader: transcript timestamps are UTC and `stat` is local, and mixing them cost a wrong conclusion about when the setting was written <!-- src:session opened:2026-08-21 runs:2 -->

## In Progress

- [ ] `VCS-020` **P2** the first bounded `autopilot` run is paused BEFORE the chain, by operator choice at the session boundary. Manifest `docs/manifests/2026-08-11-a-per-file-budget-ceiling-is-parsed-and.manifest.yml` sits at `step_0_init`/`in_progress` with `autopilot: true` and classifies `ADOPTABLE`, `SPEC.md` carries #296's spec and is modified in the working tree, and the guard is disarmed. **Relaunch, do not resume**: `autopilot-disarm.sh --completing` clears the whole transient set (ADR-0112) including `.claude/autopilot-state/scope`, so the bound is gone — `/skill autopilot --features 1 --only 296`. Phase P must be skipped again for the reason in issue #399, and the fork point is `main` because Phase P's outputs are already committed there (#401). Full handoff in `.claude/context.md` <!-- src:session opened:2026-08-11 runs:3 -->

  > **Correction (2026-08-23, ADR-0167):** `autopilot-disarm.sh` no longer clears
  > `.claude/autopilot-state/scope` — the line above names a mechanism this feature retired. A bare
  > relaunch with no `--features`/`--only` now reuses the preserved bound automatically; passing
  > them, as shown above, still works but now **replaces** it rather than being the only way back to
  > a bound. See `docs/architecture/ADR-0167-400-autopilot-disarm-scope-unbounded.md`.

## Backlog / To Add

- [ ] `VCS-038` **P3** CC 2.1.220-2.1.245 shipped features not yet evaluated for adoption here: `promptCacheTtl`/`subagentPromptCacheTtl` settings (2.1.243) would formalize in `settings.json` what `~/.claude/rules/tools.md` currently states only from empirical observation of the 1h cache window; `subagent_type: "fork"` now inherits conversation and prompt cache by default and non-teammate dispatch runs in the background by default in interactive sessions (2.1.232); the 200-subagent-per-session cap is removed (2.1.224), relevant to long `autopilot`/`project-conductor` runs; per-subagent model and effort-level are now visible in `/tasks` (2.1.243), useful for debugging the per-agent model-routing pins (sec. 3.9); `notify_when_idle` on cross-session `SendMessage` (2.1.236) could automate the "fresh session ready" half of Gate 4's `/clear`+resume flow (`~/.claude/skills/concept-to-code/SKILL.md:4285-4310`) <!-- src:session opened:2026-08-25 -->
- [ ] `VCS-041` **P3** Hooking the `InstructionsLoaded` verification (ADR-0171, proposed) to the concept-to-code chain's `[FRESH SESSION]` boundary automatically — e.g. a check that blocks advancing past Gate 4 if a recent `auto-learning` correction does not show up as loaded — is out of scope for the base proposal. It touches the skill's existing gate and needs a new transition pair in the manifest state machine, not just the observability hook itself. Recorded as a possible follow-up, not started <!-- src:session opened:2026-08-27 -->
- [x] `VCS-042` **P2** `~/.claude/skills/concept-to-code/SKILL.md` was 4,729 lines (4x the next-largest skill), loaded in full on every invocation of the primary chain workflow — the single biggest per-invocation token cost measured in the 2026-08-28 token/speed audit. Split into a slim orchestrator SKILL.md that references separate per-step files, loaded only as each step is reached, mirroring the `paths:` scoping already used for rules. Safety net landed (ADR-0172, 2026-08-28): fence-population/PAIRS-completeness globs widened to cover a future `references/` dir. Pilot extraction (Express+Hybrid paths, 203 lines) was implemented and then reverted — the full local suite (not the pre-research) found tests planting mutations directly into `SKILL.md`'s own text or greps of an exact inline sentence; a population-glob widening does not fix either, the content has to physically stay or every coupled site needs an individual repoint. 2026-08-28 follow-up (ADR-0173): measured Step 5 + §5 HITL gates at 61% of the file's bytes, confirming the benefit is real; found and hardened a second coupling class (blind `awk`-range extractions with no emptiness guard); built `staging/plugin/scripts/skill-extraction-preflight.sh`, a validated reporter that finds coupled `# plant:`/`awk`-range sites for any line range. `VCS-047` done (2026-08-28, ADR-0174): `### Step 5 — Implementation` (38.2% of the file) extracted to `references/step5-implementation.md`, byte-identical, `SKILL.md` down to 3,042 lines / 195,754 bytes. Found a third coupling class: population-derivation globs scoped to `*/SKILL.md` only, silently missing moved content — fixed in `dispatch-completion.test.sh`, `manifest-field-state.test.sh`, `transition-producer.test.sh`. **VCS-048 done (2026-08-28, ADR-0175):** `## 5. HITL gates` (23% of the post-ADR-0174 file, 1,169 lines) extracted to `references/hitl-gates.md`, byte-identical, `SKILL.md` down to 1,877 lines (75.6% smaller than the pre-`VCS-042` original). Pre-move research (16 plants / 4 awk-sites / ~11 files) undercounted the real blast radius by nearly 2x — 19 files actually broke, found by running the full suite. Confirmed both ADR-0174-discovered coupling classes generalize to independently-discovered new instances (a second negative-shaped-check widening on `concept-to-code-bsd-autopilot-gates.test.sh`'s G10, caught by `plant-check.sh`'s own PC1 pass, not by inspection; a second merged-view-splice site on `commit-transition-order.test.sh`'s line-position-based `order_check`); found a new pattern (split-invocation-and-sum, for the real multi-caller checker `path-rule-check.sh`); documented a `skill-extraction-preflight.sh` false-positive class (unrelated content shifting into a vacated numeric line range post-move). Full suite 0 failures, `plant-check.sh` 623/623 declarations fired / PASS=631 FAIL=0 / PC1–PC5b clean, byte-identical diff clean. Both `VCS-042` blocks now closed; no further `concept-to-code/SKILL.md` splits are planned — a future token-cost pass on the skill needs its own new measurement and issue <!-- src:session opened:2026-08-28 -->
- [ ] `VCS-046` **P3** 8 separate hook scripts fire per single Edit/Write (5 `PreToolUse` + 3 `PostToolUse`), each its own process spawn — pure latency overhead. Each covers a distinct documented incident, so most are not safely mergeable, EXCEPT `agent-write-scope.sh` + `test-write-scope.sh`, which share the same `agent_type`-only discriminator and bail shape. Fix: fold only that pair into one script with an early dispatch; leave the rest separate. Not started <!-- src:session opened:2026-08-28 -->
## Blocked / Decisions Needed

_none_

## Steps — project-tasks-vendored-bilateral-ledger (derived from 2026-08-17-project-tasks-vendored-bilateral-ledger.manifest.yml)

<!-- steps: no non-terminal manifest under docs/manifests — section omitted -->
## Project Map

- **Entry point**: not a code project. `docs/vibe-coding-system.md` is the blueprint; `staging/` is the deployable surface synced into `~/.claude` by `staging/sync-to-claude.sh`
- **Modules**: `staging/plugin/skills/` (32 skills) · `staging/plugin/scripts/` (hooks) · `staging/plugin/agents/` · `staging/plugin/scripts/tests/` (90 harnesses + the plant registry) · `docs/architecture/` (172 ADRs) · `docs/specs/` · `docs/superpowers/plans/` · `docs/manifests/`
- **Build & test**: no build, no lint, no package manager. test-cmd: `for t in staging/plugin/scripts/tests/*.test.sh; do bash "$t" || exit 1; done`. Mutation testing: `bash staging/plugin/scripts/tests/plant-check.sh`
- **Key ADRs**: ADR-0022 nightly-autopilot · ADR-0068 worktree isolation contract · ADR-0086 when a derived guard is extracted · ADR-0108 the plant registry · ADR-0114 required-checks audit
- **Invariants**: merge stays human, always · a check that did not run must be distinguishable from a check that found nothing (exit 3) · a checker branches on its exit code, a reporter always exits 0 and prints `CLEAN` · an assertion nobody planted pins nothing · a needle must belong to the mechanism, not to the prose describing it · measure an issue's own claim before designing

## Done

- [x] `VCS-047` Extracted `### Step 5 — Implementation` (old lines 1018-2708, 38.2% of `concept-to-code/SKILL.md`'s bytes) into `references/step5-implementation.md`, byte-identical (`diff` against `git show HEAD~1:...SKILL.md | sed -n '1019,2708p'` empty). `SKILL.md` down from 4,729 to 3,042 lines / 195,754 bytes. Used `skill-extraction-preflight.sh`'s enumerated blast radius (ADR-0173): 21 awk-range sites, ~30 whole-file greps, 20 plants, 5 fence markers, re-pointed via three mechanical patterns (simple repoint, whole-file-uniqueness concatenation, boundary-straddling concatenation) chosen per assertion's own stated semantics. Found a coupling class distinct from ADR-0172 (text-mutation) and ADR-0173 (blind-extraction): population-derivation globs scoped to `*/SKILL.md` only silently missed content moved into `references/*.md` — fixed in `dispatch-completion.test.sh` (DC21-DC25), `manifest-field-state.test.sh` (`V_WRITTEN`/`WH_WRITTEN`), `transition-producer.test.sh` (`$PROD`). One classifier (`worktree-isolation-contract.test.sh` L2/L3) needed a merged-view splice rather than a repoint, since its `awk` scan is positional and no single file has both the Step 5 heading and its body. Two files needed fixes the original plan's table did not anticipate, found only by the direct suite run (`fence-contract-coverage.test.sh` S9/E20, `hook-verify-workflow.test.sh` S7/S8). Verification: full local suite (91 files) 0 failures; `plant-check.sh` 623/623 plants fired, PC1-PC5b clean, PASS=631 FAIL=0; `pairs-completeness.test.sh` 322/322; `fence-contract-coverage.test.sh` 67/67. ADR-0174, plan `~/.claude/plans/abstract-marinating-thompson.md` (2026-08-28)
- [x] `VCS-048` Extracted `## 5. HITL gates` (old lines 1780-2948, 23% of the post-ADR-0174 `concept-to-code/SKILL.md`) into `references/hitl-gates.md`, byte-identical (`diff` against `git show HEAD:...SKILL.md | sed -n '1781,2948p'` empty). `SKILL.md` down from 3,042 to 1,877 lines (75.6% smaller than the pre-`VCS-042` 4,729-line original). Pre-move research via `skill-extraction-preflight.sh` (16 plants / 4 awk-sites / ~11 files) undercounted the real blast radius by nearly 2x — the direct suite run found 19 files broke, since the tool cannot see whole-file greps, population-glob builders, or positional cross-file classifiers. Correction to the stale TODO.md note this entry previously carried: "Gate approval recording"'s five cross-references were checked directly before the move and confirmed to live entirely inside the moved range, not outside it as an external-coupling risk. Confirmed both ADR-0174-discovered coupling classes generalize to independently-discovered new instances: a second negative-shaped-check widening, `concept-to-code-bsd-autopilot-gates.test.sh`'s G10 (a consolidated-string ban), caught only by `plant-check.sh`'s own PC1 pass on a real run, not by inspection; a second merged-view-splice site, `commit-transition-order.test.sh`'s line-position-based `order_check()`. Found a new pattern — split-invocation-and-sum — for `path-rule-check.sh`, a real multi-caller checker taking one file per invocation by design: invoked separately against `$SKILL_MD`/`$STEP5_REF`/`$HITL_REF`, requiring all three clean or summing their `occurrences=` counts. Documented a `skill-extraction-preflight.sh` false-positive class: re-running the tool with the pre-move line range against the post-move (shrunk) file matched unrelated content that shifted upward into the vacated numeric window (`batch-boundary-precedence.test.sh`'s CB4, verified to actually live in `## 7. Failure handling`). Verification: full local suite (91 files) 0 failures; `plant-check.sh` 623/623 declarations fired, PASS=631 FAIL=0, PC1-PC5b clean (two real findings fixed first — G10 via PC1, this entry's own paired `chain-decisions.md` narrative via PC5's CMC05 check); `pairs-completeness.test.sh` 324/324; `fence-contract-coverage.test.sh` 67/67. Both `VCS-042` blocks now closed; no further `concept-to-code/SKILL.md` splits planned. ADR-0175, plan `~/.claude/plans/abstract-marinating-thompson.md` (2026-08-28)
- [x] `VCS-043` `~/.claude/rules/tools.md` (264 lines, one of only two rules files with no
  `paths:` key, so always loaded) was also untracked by this repo entirely — never vendored under
  `staging/user/rules/`, never in `sync-to-claude.sh`'s `PAIRS`. Vendored first (verbatim copy,
  reviewable diff for the rewrite), then compressed all 31 entries to the `CLAUDE.md` rules-section
  shape (bolded rule, one-clause reason, bare `→ #anchor` pointer): 264 → 152 lines (42% cut),
  measured against this repo's own proven density (`CLAUDE.md`'s 3.9 lines/rule vs `tools.md`'s
  original 8.5). Original TODO note proposed also archiving "stale" entries; measuring the file
  falsified that premise — all 31 are permanent machine facts, not resolved incidents — so archived
  nothing. Dated forensics moved to new `docs/tools-evidence.md` (not loaded, `docs/` outside
  `PAIRS`), one `## <anchor>` section per entry. New `tools-rule-evidence-anchors.test.sh` guards
  the split bidirectionally (TE1 forward, TE2 backward, TE3 exact-parity denominator guard, all
  plant-declared). Found and fixed a real CI-registration gap (new test wasn't in
  `docs-ci.yml`'s harness list, caught by `pairs-completeness.test.sh`'s CI1). Also fixed a stale
  `docs/RUNBOOK.md:73` expected-`ls` listing (missing `parallelization`/`tools`). Verification:
  `pairs-completeness.test.sh` 326/326; `tools-rule-evidence-anchors.test.sh` (new) 4/4;
  `worktree-isolation-contract.test.sh` 104/104; no lesson lost (31 `- **` entries, 31 `→ #`
  anchors, checked by count); full local suite (91 files) `SUITE_DONE FAIL_COUNT=0`;
  `plant-check.sh` PASS=634 FAIL=0, `DONE rc=0`, PC1 626/626 fired across 59 files, PC5/PC5b
  clean including the three new plants. `writing-style.md` (the other untracked always-loaded
  file, 19 lines) left out of scope, not a token problem. ADR-0176, plan
  `~/.claude/plans/abstract-marinating-thompson.md` (2026-08-29)
- [x] `VCS-044` `pre-flight-pattern-enforce.sh` and `write-scope-enforce.sh` (both `PreToolUse`
  on `Edit|Write|MultiEdit`) each extracted 5 `jq` fields before checking the one field that
  decides the common-case early bail — 4 unnecessary `jq` spawns per hook per Edit/Write when
  the agent is not a coder / not a subagent, which is the overwhelming majority of calls.
  `agent-write-scope.sh` already used the correct shape (extract the discriminator alone, bail,
  extract the rest only on the live path); applied the same reorder to both hooks. Pure
  extraction reorder, no behaviour change: `pre-flight-pattern-enforce.sh` now extracts
  `AGENT_TYPE` alone first and bails on non-coder before touching `TOOL`/`CWD`/`AGENT_ID`;
  `write-scope-enforce.sh` extracts `AGENT_ID` alone first and bails on no-subagent before
  touching `TOOL`/`TP`/`FILE_PATH`. Verification: `write-scope-enforce.test.sh` 18/18,
  `pattern-enforce-transcript-source.test.sh` 17/17 (both target the staged scripts directly),
  full local suite (91 files) `SUITE_DONE FAIL_COUNT=0`. No new coupling introduced, so no
  `plant-check.sh` rerun and no ADR — same class of change as the PR #517 `stat -c`/`stat -f`
  reorder fix, plain commit only (2026-08-29)
- [x] `VCS-045` `prompt-en-prose-detect.sh` (`UserPromptSubmit`, fires on every user message)
  spawned `python3` just to read one JSON field (`.prompt`), where every other hook in the
  setup uses `jq` (~5ms startup vs ~30-50ms for Python). Replaced the `python3 -c
  "json.load..."` extraction with `jq -r '.prompt // empty'`, preserving the old try/except's
  fail-silent behaviour on malformed/empty stdin (`// empty` plus the existing `|| true`).
  The second `python3 -c` block that JSON-encodes the additionalContext output was left
  alone — out of scope, not a per-message cost (only runs on an actual EN-prose match).
  Verification: `hook-hardening.test.sh` 12/12 (its Test 4/4b/4c/4d exercise the staged
  script directly, including malformed-input paths); manual check of empty stdin and
  non-JSON stdin, both still exit 0 silent; full local suite (91 files) `SUITE_DONE
  FAIL_COUNT=0`. No new coupling, no ADR — same class as `VCS-044` (2026-08-29)
- [x] `VCS-034`/`VCS-035` Both already fixed on `main`, this ledger's entries were stale (never closed after PR #510, `fix/vcs-034-035-spec-coverage-granularity`, merged 2026-08-24, ADR-0168 written then renumbered to ADR-0169 by `VCS-040`). Verified 2026-08-26, not re-implemented: `do_pair()` in `spec-coverage-baseline-rows.sh` strips the first hyphen segment only when it is numeric (line 134, `if [ -n "$_p_n" ]`), so an issue-less SPEC's fallback slug is its whole basename — VCS-034's fix is live. `spec-coverage.sh` carries the VCS-035 line-granular filter (`OWN_LINE_RE`, `grep_boundary_test()`, lines 469-495 and 667-682), applied only to the covered/uncovered check and deliberately not to `grep_boundary_test_claimed()` (rule preserved per ADR-0169 D2). Full suite run: `spec-coverage.test.sh` PASS=176 FAIL=0, including `RS7` ("all 198 live (spec, id) verdicts match spec-coverage-scope-baseline.tsv exactly" — the previously-residual 222/R-03 divergence is gone), `RS8a`/`RS8b` clean in both directions, `RZ6`/`RZ9` (the VCS-035 fixtures) pass. No code change needed, only this closure (2026-08-26)
- [x] `VCS-040` Fixed: `docs/architecture/ADR-0168-spec-coverage-issue-less-and-line-granularity.md` (2026-08-25, PR #510) collided with `ADR-0168-commit-outcome-backstop-hook.md` (2026-08-24, PR #509). Renamed to `ADR-0169-spec-coverage-issue-less-and-line-granularity.md`, title line corrected, a dated `> **Correction (2026-08-25, VCS-040)**` note added recording the original mistaken number (rule 14, not rewritten in place elsewhere in the file). Added the missing `docs/chain-decision-index.md` entry for ADR-0169 — it had no index entry under either number before this fix. Verified: no other file in the repo referenced the collided filename or topic slug; the many other `ADR-0168` mentions across the codebase (hooks, tests, manifests, SKILL.md) all correctly cite the commit-outcome-backstop-hook ADR, which keeps its number unchanged. Two apparent further "collisions" surfaced by the same directory scan (`ADR-0012`/`ADR-0022` each having a second file) are legitimate companion documents that self-declare as such ("Reference ADR:", "Companion to:"), not competing ADRs — no action needed (2026-08-25)
- [x] `VCS-039` Implemented: new `PreToolUse`/`Bash` hook `worktree-git-guardrail.sh` denies a git invocation whose target resolves outside the worktree a dispatch was given. Detection signal is `.cwd` containing `/.claude/worktrees/`, not `.agent_type` (an agent-type allowlist would drift from dispatch-time `isolation: worktree` grants made in `concept-to-code`'s `SKILL.md`, per ADR-0068 §D6/§D7). Covers four escape vectors per git clause: `-C`/`--git-dir`/`--work-tree` flags, a preceding `cd` landing outside the worktree, an absolute-path argument, and `git worktree remove` on a different worktree; a relative target with no `..` segment is never flagged. Scope is deliberately escape-only, not a ban on mutating git (`coder`'s unrestricted `Bash` grant is unchanged). Deployed to `~/.claude/hooks/worktree-git-guardrail.sh`, wired into both `~/.claude/settings.json` and `staging/user/settings.json` `PreToolUse`/`Bash`, registered in `sync-to-claude.sh`'s PAIRS list and `docs-ci.yml`'s harness list. New paired test (`worktree-git-guardrail.test.sh`, 23 assertions incl. one `# plant:` per repo rule 2) passes standalone; `plant-check.sh` 624/624, `pairs-completeness.test.sh` 304/304, and the full 90-harness suite all pass clean. Plan approved via plan mode (`~/.claude/plans/jazzy-juggling-firefly.md`) (2026-08-25)
- [x] `VCS-036` Decided: set `CLAUDE_CODE_ENABLE_TODO_TOOLS=1`. Analysis found no dependency on `TaskCreate`/`TaskUpdate`/`TaskList`/`TodoWrite` anywhere in this system (`project-tasks` is the only reference, and only to disclaim overlap); the CC 2.1.233 changelog entry states the default-off change with no rationale, so none is asserted here. Applied to `~/.claude/settings.json` env block and mirrored in `staging/user/settings.json` (2026-08-25); effect starts next session, same process-start-read pattern as `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE`. Note: `staging/user/settings.json` still has other pre-existing drift from the live file (missing `CLAUDE_CODE_FILE_READ_MAX_OUTPUT_TOKENS`, `MAX_MCP_OUTPUT_TOKENS`, the `ANTHROPIC_CUSTOM_MODEL_OPTION*` keys), out of scope here, not fixed (2026-08-25)
- [x] `VCS-037` Verification complete against CC 2.1.220-2.1.245's fixes: (1) 2.1.222 worktree-isolation destructive-git escape — CLOSED, we were exposed pre-fix (`coder`/ADR-0068 relies entirely on platform isolation, no compensating control), no action needed now but the gap itself is tracked forward as `VCS-039`; (2) 2.1.222 `PreToolUse` background-task bypass — CLOSED, `stop-gate.sh`/`pre-flight-pattern-enforce.sh`/`protect-files.sh`/`db-backup-guardrail.sh` are fail-open by contract with no invocation trace, a past bypass is structurally unverifiable, no action possible; (3) 2.1.243 `Bash(...)` pattern false-trigger on `$()`/backtick — INCONCLUSIVE, `architect`/`reviewer` tool-scope patterns are standard and not over-wide, changelog text insufficient to tell false-permission from false-block direction, no matching historical symptom; (4) 2.1.238 subagent-result memory leak — CLOSED, confirmed platform-side, no action; (5) 2.1.234 lost session-scoped permission responses on background subagent prompts — INCONCLUSIVE, no matching symptom in this ledger or the ADRs (ADR-0020 documents a related but distinct 2.1.186-era permission-prompt stall) (2026-08-25)
- [x] `VCS-033` residue closed: `docs/vibe-coding-system.md` line 1891's stale "session default is `effortLevel: high`" claim is corrected forward (rule 14, not rewritten in place) with a dated `> **Correction (2026-08-22, VCS-033)**` block noting the live/staging drift (`medium` vs `high`) and that the coincidence with the `opusplan` fix stays a hypothesis, not a confirmed mechanism (2026-08-22)
- [x] `VCS-022` The skill is vendored under `staging/plugin/skills/project-tasks/` with its seven PAIRS lines, and the `deployed-only: project-tasks` declaration is deleted, in one commit because either half alone turns `DO2` red. `sync-to-claude.sh` no longer reports it under "deployed skill(s) neither vendored nor declared". ADR-0153, Task 2 (2026-08-18)
- [x] `VCS-023` `scan.sh`'s marker predicate is now a declaration form plus a comment leader earlier on the same line, so neither measured false positive fires. Verified in both directions and against the real repository: `SPEC.md` 3 to 0, `untrusted-input.test.sh` 1 to 0, `recovery-preflight.test.sh` 1 to 0, five before and zero after, with genuine markers still firing. ADR-0153, Task 3 (2026-08-18)
- [x] `VCS-015` Filed as issue **#398** and therefore leaves this ledger. `commit` Step 6b maps `rc=0` from `timeout 480 gh pr checks --watch --fail-fast` to "CI green — all required checks passed" and proceeds to the merge proposal, but `gh` exits 0 when the WATCH dies, not only when every check passed. Observed live on PR #397: `net/http: TLS handshake timeout` with `shell-tests` still `pending` and `mergeStateStatus: UNSTABLE`. The `rc=124`/`rc=8` branch immediately below already models "still pending" correctly — the gap is only that a dead watch does not land there (2026-08-11)
- [x] `VCS-016` Filed as issue **#399** and therefore leaves this ledger. `autopilot` Phase P step 3 iterates every `_issue-map.tsv` row whose SPEC is absent, reading neither the roadmap state nor the run scope: 74 rows, 4 missing, all four already completed, so it would generate SPECs for closed issues into `docs/specs/` — the namespace ADR-0106 uses as the archive. `--only` cannot bound it by design (ADR-0129 §D7 keeps Phase S away from `PROJECT.md`) (2026-08-11)
- [x] `VCS-018` Filed as issue **#400** and therefore leaves this ledger. `autopilot-disarm.sh` clears `.claude/autopilot-state/scope` with the marker — ADR-0112 defined the whole-set clear before ADR-0129 put the scope file in that directory. The hazard is the relaunch: a bare `/skill autopilot` after a disarm runs UNBOUNDED, and nothing says the bound is gone. Same shape as the defect ADR-0112 closed, pointed the other way — there a leftover file blocked, here a removed file silently permits (2026-08-11)
- [x] `VCS-017` Filed as issue **#401** and therefore leaves this ledger. `autopilot` Phase P step 4 has no defined `_prep_ref` when steps 1-3 all skip: with nothing changed the `commit` skill stops on "Nothing to commit", and the fork point every feature branch depends on is left to accident — the exact thing ADR-0127 §D4 exists to prevent. The `prep:`-absent case IS handled; the gap is the marker that has a `prep:` block whose every step skips (2026-08-11)
- [x] `VCS-019` Filed as issue **#402** and therefore leaves this ledger. `_issue-map.tsv` is written `slug \t issue \t title` (`roadmap-from-issues.sh:107`) and no consumer says so. Reading the first two columns in the other order does not fail — it reported **74 missing SPECs against a true 4**, and was caught only because a file it called missing demonstrably existed (2026-08-11)
- [x] `VCS-001` `ADR-0126-293-task-identifier-letter-suffix.md` (325 lines, `Status: Proposed`) and the aborted chain's manifest are on `main`, recovered from `feat/task-num-extracts-digits-only-so-a-lette` before that branch could be pruned. The exposure was measured, not assumed: a cleanup pass on 2026-08-10 deleted 23 merged branches and this one was the single candidate the verification saved — it held the only copy of a document `TODO.md` already cited. All ten of the ADR's cross-references resolve against `main`, and the manifest validates (`aborted`/`aborted` is terminal, so ADR-0078 exempts its developer-machine `project_root` and CI does not fail on it). Issue #293 stays open and the ADR stays `Proposed`; what is still missing is the plan, which is `VCS-002` (2026-08-10)
- [x] `VCS-014` The #385 chain ran to completion and merged in PR **#393** at `eb774be`, so the local-only exposure it shared with `VCS-001` is resolved: `ADR-0132`, the 9-task plan and the SPEC are all on `main`. The branch was deleted on 2026-08-10 after verifying by content rather than by ancestry — the Step 7 snapshot collapse orphans the merge history, so `git branch --merged` reports nothing and proves nothing. What the chain then exposed is issue #394, merged in PR **#395** at `e1f00a5`: the same argument-substitution class one layer down, where a fence is executed by the host shell rather than by bash (2026-08-10)
- [x] `VCS-013` The #365 chain ran to completion and merged in PR **#382** at `58e9486`. The local-only exposure it shared with `VCS-001` is resolved: `SPEC.md`, `ADR-0129` and the 9-task plan are all on `main`, and the branch survives at `origin/feat/365-scope-and-bound-the-autopilot-run` as well. What the shipped bound then proved is separate and lives in `PROJECT.md` Phase 12, not here: the first run under it (`--features 1 --only 294`, PR #383) refused #293 `OUT-OF-SCOPE` and stopped at `EXHAUSTED 1/1` without writing `[~]`, a `skipped-features` entry or `needs-human` (2026-08-08)
- [x] `VCS-008` The autopilot reposition is no longer mid-chain: ADR-0127 Parts 1-3 (#363, #364) and Part 4 (#365, PR #382) are all merged. The superseded clause stands as recorded — the `token-budget` producer was **not** built and the halt was removed outright (ADR-0129 §D6), because a ceiling checked at publish cannot stop the feature that breached it; `rtf-blocker` keeps its mechanism and its corrected sentence, tracked as issue #374 (2026-08-08)
- [x] `VCS-006` Filed as issue **#380** and therefore leaves this ledger, which holds only what is not already a GitHub issue. Re-measured before filing and the entry's own numbers were understated: the file is 3,592 lines / 274 KB / ~68,500 tokens and **98%** of it is 90 ADR blocks, against 80 lines of everything else. The cost claim is now measured rather than asserted — ~21.5% of the orchestrator's cache-read volume, ~$1,616, about four times the coder's entire cost. Sub-agents are unaffected: they do not load the project `CLAUDE.md` (ADR-0130 `## Correction`) (2026-08-06)
- [x] `VCS-007` Issue #370, ADR-0128, merged in PR #371 at `5af6e55`: `publish-feature.sh --issue <N>` puts `Closes #N` in the body and `project-conductor` reads the number off the roadmap line before the checkbox flip. The mechanism is shipped but **unproven end to end** — #370 closed because a human wrote the keyword into #371 by hand; the first unattended PR is the evidence, tracked as Phase 11 Wave 2 (2026-08-06)
- [x] `VCS-009` The ADR-0127 rename is deployed: `~/.claude/settings.json` names `autopilot-guard.sh`, zero stale `nightly-guard.sh` references, zero `~/.claude/hooks/nightly-*.sh` remaining, `sync-to-claude.sh --apply` proceeds (2026-08-05)
- [x] `VCS-005` PRs #362, #367 and #368 all merged; zero PRs open (2026-08-05)
- [x] `VCS-003` Five Phase-P SPECs reached `main` — #335, #336, #350, #355 and #358 all present under `docs/specs/` on `origin/main` since PR #362 merged (2026-08-05)
- [x] `VCS-010` Answered by ADR-0129 §D6, which states in terms so a future edit does not tidy it away: `rtf-blocker` is neither given a producer nor removed — the mechanism is kept, byte-unchanged, with its unreachability documented and measured (`concept-to-code` Gate 5's autopilot default is "Skip review", so no review cycle runs on an unattended roadmap run at all). The larger question this uncovered — whether the unattended path should review — is out of scope for #365 and stays open as issue #374, which needs no separate TODO entry since it already has a GitHub issue number (2026-08-06)
